# PROCESS

反回归笔记和可复用经验。状态快照见 PROJECT_STATUS.json。

## 2026-05-11

- **冻结规则外新增“可执行风控版”纸面对照**：用户希望并行观察一个非涨停追高过滤版，而不是改动当前冻结主线。实现为 `market-factor-controls-v4-2026-05-11`：正式冻结策略仍按 `低换手上升趋势` 第1名生成；新增 `可执行风控版` 对照沿用同一低换手上升趋势排序，但排除信号日涨幅达到 `9.5%` 及以上的候选后再取第1名，进入市场因子纸面对照和同一四轨退出监测。目的仅是观察“避免涨停后次日追高不可执行/滑点风险”是否改善前向表现，不反向调整冻结策略。
- **5/8 盘前可用名单已作为 5/11 入场纸面 seed 补齐**：用户指出 2026-05-08 收盘数据已经给过 2026-05-11 开盘盯盘名单，因此 5/11 收盘后不应显示完全没有纸面跟踪记录。复查确认正式 `shortpick_experiment_runs/shortpick_candidates` 在 5/11 前没有 tracking role 记录，但 `output/shortpick-mainboard-new-retail-tushare-20260510.db` 和组合回测产物能复现该名单，包含 `601138.SH 工业富联` 5/8 收盘 63.28、1日 -1.4%、10日 +3.0%、20日 +20.3%、换手 0.8165%。修复为：新增可重复执行回填脚本 `scripts/backfill_shortpick_prefreeze_20260508_paper_tracking.py`，将 5/8 信号、5/11 入场的冻结规则和市场因子对照写成显式 `prefreeze_20260508_open_watchlist` seed；`/shortpick-lab/paper-tracking` 支持读取冻结前但有 tracking role 的 seed，并向前端返回 `signal_date/entry_date`；前端纸面跟踪使用这两个字段显示“已入场记录”，同时 5/11 盘后正式批次仍显示为 5/12 下轮选择。验证：`pytest -m runtime_integration tests/test_shortpick_lab.py::ShortpickLabTests::test_paper_tracking_includes_prefreeze_seed_dates` 通过，`npm run build` 通过。repo DB 和 runtime DB 均已执行回填，各插入 1 个 seed run + 8 个纸面候选。
- **LLM 纸面对照必须先过新开户主板可买过滤**：用户发现 `300604.SZ 长川科技` 被选为当天 LLM 纸面对照，但当前 `new_retail_cash_account` 口径只允许沪深主板普通 A 股，创业板/科创板/北交所/ST 都不能进入严格交易对照。根因是 LLM 纸面对照选择器只排除了系统 market-factor overlay，没有复用账户可交易性过滤。已修正为：全量 LLM 推荐池继续保留为研究样本，但标记 `llm_paper_control_primary` 前必须通过 `account_trade_eligibility(..., account_profile=new_retail_cash_account)`；被排除的候选会记录 `excluded_examples` 和板块原因。当天 runtime DB 已对 `run 121` 重新选择，清除 `300604.SZ` 的 LLM 对照标记，新的 LLM 纸面对照为 `600236.SH 桂冠电力`，`300604.SZ` 仅保留为普通 LLM 研究候选。

## 2026-05-10

- **主板新账户长样本已补齐三大指数参照**：用户指出“当前 180 只主板样本库没有沪深300/中证500/中证1000指数序列”。根因是专用 Tushare 样本库只包含 180 只股票，`benchmark_references` 只能返回 `missing_series`；主库虽有指数，但只覆盖 2025-03-27 起，不足以支撑 2023-05 起长样本。已用 `sync_benchmark_index_bars(lookback_days=1400)` 向 `output/shortpick-mainboard-new-retail-tushare-20260510.db` 补入三大指数各 926 根日线，覆盖 2022-07-11 至 2026-05-08，并重跑 `output/shortpick-portfolio-backtest-new-retail-mainboard-current-20260510.json`。当前指数参照：沪深300 +19.73% / 最大回撤 -24.09%，中证500 +35.75% / 最大回撤 -31.09%，中证1000 +25.47% / 最大回撤 -38.85%；等权市场基准仍作为策略超额主基准。前端市场参照卡片改为同时展示三大指数；测试新增三大指数必须 available 的约束。
- **正式验证前检查发现并修正历史回放展示缺口**：冻结主策略逻辑仍为 `shortpick-v4-low-turnover-uptrend-2026-05-11`，未来正式纸面跟踪按低换手上升趋势写入，旧第二候选只作为对照。预验证中发现历史回放的“策略收口和完整统计表”只投影了部分组合回测行，且指数序列未纳入当前 180 只主板样本库时容易被前端理解成 0%。已补齐冻结主线、旧主线、旧主线止损、强广度二候选、前三名等权、金叉、原动量换手、降追高等组合回测行；市场参照改用等权市场基准，并显式说明指数序列缺失，避免用 0% 误导。
- **冻结主策略已从旧第二候选切到低换手上升趋势**：发现前端仍显示 `冻结纸面策略：第二候选四轨退出监测` 后，确认上一轮只把低换手规则接成纸面对照，没有切换 frozen primary。已修正为 `shortpick-v4-low-turnover-uptrend-2026-05-11`：正式冻结主策略为“低换手上升趋势四轨监测”，旧第二候选降级为市场因子对照。历史回放 leading evidence 已改指向 `low_turnover_20d_uptrend_liquid_top120`，新开户主板长样本保持收益 +87.17%、等权市场基准 +38.14%、超额 +49.03%、最大回撤 -27.72%；旧第二候选加8%止损为收益 -49.04%、超额 -87.18%、最大回撤 -63.71%。前端纸面跟踪和历史回放文案应以低换手为当前冻结主线。
- **短投历史优化的新增中止信号**：当原动量追涨口径在新开户主板长样本下整体失效时，可以发散到“非拥挤趋势”而不是继续微调动量阈值。本轮有效候选为“低换手上升趋势”：180 只主板可交易样本、2023-05-16 至 2026-04-27、715 个信号日、日滚动 5 日持有，收益 +87.17%，同期等权市场基准 +38.14%，超额 +49.03%，最大回撤 -27.72%，且 2023/2024/2025/2026 分年超额均为正。规则参数已进入 `default_policy_configs.py`，并作为冻结主策略接入纸面跟踪。临时搜索里“安静突破二候选”无法在正式回测口径复现，不能进入纸面跟踪。
- **历史回放 strict/source 与 account-executable 口径已拆开**：用户追问为什么 DS V4 Pro 直选完成 22 个日期后，前端仍像只有少量正式样本。根因是 replay feedback 只用 `official_sample_eligible` 统计；LLM 直选很多候选没有逐条 official source/evidence mapping，会因 `unsupported_claim` 退出严格来源样本，但这不等于它不可交易或存在未来泄漏。本轮保留严格来源口径，同时新增“可交易验证样本”口径：排除 `source_not_in_packet / source_after_cutoff / unverified_source_time / future_leakage_suspected` 等硬泄漏和不可交易标的，但允许仅因 `unsupported_claim` 降级的行情快照候选进入实盘可执行验证。运行态 `/shortpick-lab/replay-feedback` 现在返回 LLM strict/tradable 分层：LLM 5日严格来源 `5` 样本、平均超额 `-2.0%`；可交易验证 `103` 样本、平均超额 `+2.4%`。全局历史回放首屏显示严格来源样本 `81`、可交易验证样本 `2231`，并在周期卡/组别表同时展示两种口径。验证：`pytest tests/test_shortpick_replay.py tests/test_shortpick_portfolio_backtest.py -q` 为 `14 passed`，`pytest tests/test_frontend_shortpick_static.py -q` 为 `2 passed`，`npm run build` 通过；已手动同步到 runtime 并重启服务，Playwright+Node 24 验证 `http://127.0.0.1:5173/?verify=strict-tradable -> 试验田 -> 历史回放` 显示 `严格来源 / 可交易验证` 且 console error 为 0。后续解释研究结论时必须明确区分“证据严格成立”和“当时按账户约束可买入后的收益”。
- **DS V4 Pro 新账户直选样本继续扩到 32 个历史日期**：在 strict/tradable 口径拆开后，用 2 并发补跑 `2026-01-05..2026-01-16`，写出 `output/shortpick-replay-new-retail-ds-stage1-shard3-20260510.json`，10 个日期全部 completed，`failed_llm_count=0`。运行中 `/shortpick-lab/replay-feedback` 仍在约 `1.4~1.7s` 返回，未观察到 live DB 锁。默认聚合现在覆盖 `32` 个新账户 replay 日期，LLM 直选候选 `156`，其中可交易验证完成 `153`；LLM 可交易平均超额为 `1d +0.63% / 3d +1.65% / 5d +1.59% / 10d +2.37% / 20d +3.86%`，但正超额比例仍约 `47%~52%`，说明当前信号更像少数强尾部拉动均值，尚不能按稳定胜率策略解读。
- **新开户账户口径下重新开启 DS V4 Pro LLM 回放验证**：历史回放默认 universe 已接入 `new_retail_cash_account`，只允许沪深主板普通 A 股，排除科创/创业/北交/ST；`shortpick-replay` 新增 `--account-profile` 和 `--llm-max-workers`，DS 直选请求并发、SQLite 写入串行。`shortpick-replay-distill` 也新增并发蒸馏入口，并改为同一 run 的自选池/动量池蒸馏结果凑齐后立即串行写库。真实 DS 路由确认仍为 `deepseek-v4-pro[1m]`，不走 flash。实测 4 并发在当前 DS/API 链路上会长时间无返回，因此执行策略降为小分片 2 并发。已完成新账户 DS 直选回放 22 个日期（probe `2026-04-21..04-22`，shard1 `2026-03-02..03-13`，shard2 `2026-02-02..02-13`），直选候选 5日均值 `+2.08%`、5日平均超额 `+2.27%`，10日均值 `+1.55%`、10日平均超额 `+2.91%`，但中位数仍为负，不能作为生产级证明。蒸馏因 DS 吞吐较慢仅完成 2 个日期探针，不作为完整结论。默认聚合反馈现在只纳入新账户口径 run，旧宽口径历史数据仍保留但不参与默认前端结论；组合回测 artifact 指向 runtime DB 重跑的 `output/shortpick-portfolio-backtest-new-retail-mainboard-current-20260510.json`。
- **朋友建议历史回测结果从独立卡片归入组别表**：用户指出 `前三名等权组合` 与 `10/200日金叉过滤` 虽然有历史数据卡片，但没有在历史回放的“组别”里体现。本轮删除这两条的独立朋友建议卡片，把 portfolio backtest 的长样本结果投影到 `试验田 -> 历史回放 -> 策略收口和完整统计表` 的组别表中，并用 `口径=组合回测` 区分于原有 `候选回放` 行；表内显示交易次数、长样本超额、总收益、最大回撤和规则说明。顶部“当前领先组”仍只按候选级 5 日回放比较，避免把组合回测的长样本超额和 5 日均值混排。验证：`PYTHONPATH=src pytest -q tests/test_frontend_shortpick_static.py` 为 `2 passed`，`npm run build` 通过。发布使用临时快照 `/tmp/ashare-friend-controls-groups-publish.20260510181818`，commit `9a71365e7b55f6b6fdaa98c287e5b17995ec4ce5`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260510T101903Z-9a71365e7b55/manifest.json`，deploy verifier `19 passed, 0 failed`。真实页面已验证组别表包含 `前三名等权组合 / 组合回测 / 1,150 笔交易 / -2.9% 长样本超额 / 收益 +92.2% / 最大回撤 -29.7%` 和 `10/200日金叉过滤 / 组合回测 / 132 笔交易 / -95.1% 长样本超额 / 收益 -0% / 最大回撤 -13.9%`；截图 `output/playwright/shortpick-friend-controls-groups-runtime.png`。发布未触发 post-deploy 数据刷新。
- **朋友建议的两条短投规则已作为诊断对照组发布，而不是升级成主线**：评估后认为两条建议都有研究价值：前三名等权能检验“单票第2名”是否只是偶然，10/200日金叉能检验趋势确认是否能降低伪动量。但长样本回测不支持替换当前冻结主线。基于 `output/shortpick-portfolio-backtest-optimized-v2-long-sample-20260509.json`，`daily_rolling_5x10k` 口径下，当前冻结主线 `ret10_turnover_second_market_positive_cooldown_stop8` 仍为长样本收益 `+157.7%`、等权市场超额 `+62.6%`、最大回撤 `-27.2%`、交易 `385` 次；新增 `前三名等权组合` 为收益 `+92.2%`、超额 `-2.9%`、最大回撤 `-29.7%`、交易 `1150` 次；新增 `10/200日金叉过滤` 为收益约 `0.0%`、超额 `-95.1%`、最大回撤 `-13.9%`、交易 `132` 次。结论：前三名等权降低了单票依赖但明显稀释 alpha；金叉过滤过于滞后且样本稀疏，适合作为风险/趋势确认对照，不适合作为当前主线。
- **纸面跟踪和历史回放已同步新增朋友建议对照**：`shortpick_lab` 纸面跟踪合同升级到 `market-factor-controls-v2-2026-05-10`，新增 `前三名等权组合` 与 `10/200日金叉过滤` 两条市场因子对照。未来 live overlay 在 gate 通过时会记录前三名等权候选，并在动量成交量候选顺序中选择首个触发 10日均线上穿200日均线的标的；两者与正式策略共享同一入场和四轨退出监测，不调用模型、不触发额外刷新。历史回放前端 `策略收口和完整统计表` 增加 `朋友建议对照` 卡片，直接展示两条规则的长样本超额、收益、交易次数和最大回撤。验证：`py_compile` 通过；`PYTHONPATH=src pytest -q tests/test_shortpick_portfolio_backtest.py tests/test_frontend_shortpick_static.py tests/test_policy_governance.py` 为 `14 passed`；`PYTHONPATH=src pytest -q tests/test_shortpick_lab.py -m runtime_integration` 为 `35 passed`；`npm run build` 通过；`PYTHONPATH=src python3 -m ashare_evidence.policy_audit --fail-on-new-unclassified` 通过。发布使用临时快照 `/tmp/ashare-friend-controls-publish.20260510180222`，commit `be1451d2f3c589d875b59f4f0d20a2a2e234dbbe`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260510T100257Z-be1451d2f3c5/manifest.json`，deploy verifier `19 passed, 0 failed`。真实页面已验证 `试验田 -> 纸面跟踪` 显示两条新增规则，`试验田 -> 历史回放` 显示 `前三名等权组合` 与 `10/200日金叉过滤` 回测卡片；截图 `output/playwright/shortpick-friend-controls-runtime.png`。发布未触发 post-deploy 数据刷新，清理浏览器自动化后未发现 refresh/shortpick/DeepSeek/Claude 进程残留或 `run.lock`。

## 2026-05-09

- **历史回放与相关接口加载态已收口并发布**：用户指出历史回放里 `-` 会让人分不清“无数据”还是“加载中”。本轮把 `试验田 -> 历史回放` 的全局统计、策略收口、回放批次列表、候选明细、封闭数据包/来源清单、下钻详情全部改为接口级加载态：请求未完成时显示“加载中”或骨架屏，确认无数据时才显示“暂无数据/暂无来源清单/当前没有可展示数据”。同时补齐首页/移动端 `冻结纸面跟踪` 请求加载标签，以及 `运营复盘` 里懒加载的人工研究队列和焦点研究工作区骨架屏，避免接口返回前误报空状态。验证：`npm run build` 通过，`PYTHONPATH=src pytest -q tests/test_frontend_shortpick_static.py tests/test_frontend_scheduled_refresh_static.py` 为 `3 passed`。发布使用临时快照 `/tmp/ashare-loading-states-publish.20260510010435`，commit `0025b55005904f801b082f67f4445fac39065ffa`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T170500Z-0025b5500590/manifest.json`，deploy verifier `19 passed, 0 failed`。真实页面已验证历史回放初始态显示“历史回放数据加载中 / 全局统计加载中 / 样本数加载中 / 正超额加载中”，加载完成后显示正式统计、候选表和来源清单；`运营复盘` 页面也能正常渲染人工研究队列。截图 `output/playwright/loading-states-shortpick-runtime.png`。发布与验收未触发数据刷新，清理浏览器自动化后未发现 refresh/shortpick/DeepSeek/Claude 进程残留，refresh state 目录无 `run.lock`。
- **短投冻结策略参数治理已补齐并发布**：审计发现正式冻结策略的池子大小、TopK、冷却惩罚、市场 gate、退出阈值和纸面对照版本虽然有命名常量/合同函数，但没有完全进入 2026-05-07 确定的 policy governance。修复后新增 `shortpick_lab.frozen_paper_strategy_v1` 默认治理配置与 `shortpick_policy.py` 入口，`shortpick_lab.py` 只投影使用配置值；policy audit 新增 `shortpick_frozen_paper_strategy` 分类，并在 `--fail-on-new-unclassified` 下拦截关键短投阈值回到裸字面量。测试：`tests/test_policy_governance.py` 10 passed、`tests/test_shortpick_lab.py -m runtime_integration` 35 passed、policy audit pass、`npm run build` pass。发布使用临时快照 `/tmp/ashare-shortpick-policy-governance-publish.20260510005001`，commit `438621d9706c75c3e7da91bc9eebef3410de6d48`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T165028Z-438621d9706c/manifest.json`，deploy verifier `19 passed, 0 failed`。运行态 `/policy-governance/audit` 返回 `status=pass`，`/policy-governance/active` 已显示 `shortpick_lab.frozen_paper_strategy_v1`；真实页面 `运营复盘 -> 参数与公式治理` 已验证默认配置数为 6、硬约束失败 0，并显示 `shortpick_frozen_paper_strategy`；截图 `output/playwright/shortpick-policy-governance-runtime.png`。发布后无 refresh/shortpick/DeepSeek/Claude 进程残留，未发现 `run.lock`。
- **大改后规则回归审计发现两个小偏移并修复**：一次整体回顾后确认 paper-tracking API 仍是只读，不触发行情同步、模型调用或 DB 写入；前端进入试验田仍不会自动触发全量市场研究。发现并修复两个隐患：1) 无 live run 但已有 paper-tracking 合同时，workspace tab activeKey 会固定成 `纸面跟踪`，用户可能无法切到 `历史回放`；2) 市场因子纸面对照进入四轨退出，但候选展示周期仍是 5 日，和 10 日四轨监测语义不一致。修复后无 live run 时只把初始 tab fallback 到纸面跟踪，用户切换后保持所选 tab；市场纸面对照的 `horizon_trading_days` 改为 10，普通未跟踪市场候选仍为 5。发布使用临时快照 `/tmp/ashare-post-change-audit-publish.20260510004040`，commit `5a5ddd68bf27ef14a0058d5a55c3329e0fdf93e7`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T164119Z-5a5ddd68bf27/manifest.json`，deploy verifier `19 passed, 0 failed`；真实页面 `试验田 -> 纸面跟踪` 已验证无 console error，截图 `output/playwright/shortpick-post-change-audit-runtime.png`。发布后无 refresh/shortpick/DeepSeek/Claude 进程残留，refresh state 目录无 `run.lock`。
- **最终冻结前对照组已发布收口**：最后一次修改窗口决定不把单票分析升入 live paper tracking，原因是当前 `recommendations` 覆盖面太窄，历史 LLM rejector / hard-veto 也没有证明增益。正式策略继续固定为“动量成交量 Top40 池 + 进攻排序第2名 + gate + 四轨退出监测”；新增三条低噪声同池对照：`动量换手第1名`、`降追高第1名`、`同池随机基线`。这些对照不调用模型、不触发额外刷新，和正式策略/LLM一票对照共享同一入场与四轨退出。发布使用临时快照 `/tmp/ashare-final-controls-publish.20260509233736`，commit `cfbbcd1d42d143230cfd936edd3e5ec0a04c8db7`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T153817Z-cfbbcd1d42d1/manifest.json`，deploy verifier `19 passed, 0 failed`。运行态 API 返回 `market-factor-controls-v1-2026-05-09`，真实页面 `试验田 -> 纸面跟踪` 已验证显示市场因子对照规则、随机同池基线和“单票分析当前覆盖不足”边界说明；截图 `output/playwright/shortpick-final-controls-runtime.png`。发布后检查无 refresh/shortpick/DeepSeek/Claude 进程残留，refresh state 目录无 `run.lock`。
- **LLM 对照从推荐池升级为严格一票纸面对照**：每个 live shortpick run 在 LLM 共识完成后，会从当日 LLM 自由推荐池按固定规则提前标记 1 只 `LLM纸面对照` 标的，规则为跨模型同票优先，其次同模型重复、跨模型同题材、单模型高置信、系统外新视角，再按来源质量、置信度、来源数量、股票代码和候选ID稳定排序。这个标的不改变全量 LLM 推荐池的研究用途，但会进入 `/shortpick-lab/paper-tracking`，并在 10 个交易日窗口完整后和冻结策略一样写出机械5日、机械10日、条件检查、10%触达止盈四轨结果。前端 `今日批次` 增加 `LLM纸面对照标的` 卡，`纸面跟踪` 增加 `LLM纸面对照规则` 和正式策略/LLM对照混合记录表；当前 run 30 早于规则，不会被 retroactive 标记。发布使用临时快照 `/tmp/ashare-llm-one-stock-control-final.20260509223536`，commit `fc481614ce29a9fc719b294bb35318c36533bb0c`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T143618Z-fc481614ce29/manifest.json`，deploy verifier `19 passed, 0 failed`。运行态 API 返回 `llm-paper-control-v1-2026-05-09`，真实页面 `试验田 -> 纸面跟踪` 已验证显示 LLM 一票对照规则和研究池边界说明；截图 `output/playwright/shortpick-llm-one-stock-control.png`。发布后无 refresh/shortpick 进程，refresh state 目录无 `run.lock`。
- **冻结纸面跟踪已在首个样本前改为四轨退出监测**：冻结策略合同升级为 `shortpick-v3-exit-tracks-2026-05-09`，同一只正式纸面标的会并行记录 `机械5交易日`、`机械10交易日`、`5到10交易日条件检查`、`10%触达止盈` 四条退出结果。所有 5/10 日窗口均按交易日计算，不按自然日；10% 止盈在当前日线数据下以 daily high 触达作为纸面近似，触达则按 +10% 收益退出。该调整发生在 `waiting_first_frozen_run` 之前，没有已有冻结样本需要迁移。后端验证 snapshot 会在 10 个交易日窗口完整后写入四轨结果，前端 `纸面跟踪` tab 的规则卡和未出首批状态已同步显示四轨口径。本轮发布使用临时快照 `/tmp/ashare-four-exit-tracks-final.20260509220826`，commit `27e6de42993638f5261025e390e65a8a3ee5b4eb`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260509T140905Z-27e6de429936/manifest.json`，deploy verifier `19 passed, 0 failed`。运行态 API 返回四轨合同，真实浏览器验证 `试验田 -> 纸面跟踪` 无缺失文案和 console error；截图 `output/playwright/shortpick-four-exit-tracks-closeout.png`。发布后无 refresh/shortpick 进程，refresh state 目录无 `run.lock`。
- **冻结纸面策略与 LLM 对照批次已完成前端/运行态分轨展示**：新增只读 `/shortpick-lab/paper-tracking`，从已有 `shortpick_lab` run/candidate 表和冻结策略合同生成 paper-tracking ledger，不触发行情同步、模型调用或数据库写入。运行态当前返回 `waiting_first_frozen_run`：最新批次为 `2026-05-08` run `30`，生成于冻结日期 `2026-05-09` 前，`has_frozen_overlay=false`，因此页面明确显示“等待首个冻结后正式批次”。前端同步改造：首页每日状态把 `shortpick_lab` 标成 `LLM对照批次`，额外显示 `冻结纸面跟踪：等待首批`；全局导航说明改为“冻结纸面策略、纸面跟踪和LLM对照组”；`试验田` 新增 `纸面跟踪` tab，按钮改为 `生成LLM对照批次 / 补跑对照复盘`，LLM 历史验证和模型反馈明确标成对照组。发布使用临时快照 `/tmp/ashare-paper-tracking-closeout.20260509160020`，commit `02ae368fb66a12e4e5d1f25ca22816e7c337aa82`，`ASHARE_PUBLISH_REFRESH_MODE=skip`，deploy verifier `19 passed, 0 failed`。运行态 API、scheduled-refresh status 和真实浏览器页面均已验证；截图 `output/playwright/shortpick-paper-tracking-closeout.png`。发布后检查无 `ashare_evidence.cli`/shortpick/refresh 进程运行，refresh state 目录无 `run.lock`，没有额外刷新或 DB 锁。
- **DeepSeek v4 pro 搜索链路不能只依赖本机 SearXNG 引擎可用性**：2026-05-08 live run 的 DeepSeek 失败根因是 LobeChat/SearXNG endpoint 可响应但外部 engines 全部 timeout/access denied，导致 query planning 正常、实际搜索结果为 0。修复为 DeepSeek executor 仍先用本机 SearXNG，但当结果为空或后端不可用时自动走 Sogou Web 公开搜索后备；最终回答仍只能引用系统搜索结果中的 URL，`final_source_not_in_search_results` 继续 fail-closed。真实探针用 `2026-05-08` 短投任务跑通 `deepseek-v4-pro` plan→fallback search→final JSON，返回 20 条搜索结果并生成有效候选，trace 会标记 `lobechat_searxng_with_sogou_fallback` 和 `sogou_web_fallback` engine。runtime run `30` 已用单轮失败重试补齐 DeepSeek `5/5` 轮，run 状态为 `completed`，LLM 候选从 21 补到 22。
- **短投试验田正式接入历史验证后的 market-factor 策略，同时保留 LLM 对照组**：live `shortpick_lab` run 现在先让 GPT/DeepSeek 完成独立开放检索选股并构建 LLM 共识，再在共识之后写入两组系统策略候选：默认 `momentum_10d_turnover_cooldown_rank`，进攻对照 `momentum_10d_turnover_rank`。策略候选作为 first-class candidates 进入同一套 `1/3/5/10/20` 后验验证，但 `build_shortpick_consensus()` 明确排除 `candidate_origin=market_factor_overlay`，避免系统策略污染“LLM 是否自发收敛”的核心读数。当前 runtime run `30`（`2026-05-08`）已补入 12 条策略候选；现有研究股票池补到 2026-05-08 后有效策略池为 61 只，默认/进攻各 6 只候选均有 5 个 horizon 的 pending-forward 验证行。LLM 选股继续保留为对照，DeepSeek v4 pro 的 5 轮搜索失败已通过 Sogou fallback 重试补齐，并在 trace 中保留真实搜索后端来源。
- **试验田页面不得触发全量市场研究或长事务补数**：策略池行情补齐只发生在 Short Pick Lab run 生成/手动补 overlay 时，不发生在面板打开、tab 切换、历史回放聚合或模型反馈加载时。补现有研究股票池时逐票 fetch/upsert 并 `commit`，避免网络等待期间持有长 SQLite 写锁。页面进入 `试验田` 仍保持轻量 run list，历史回放和 market-study 聚合后台加载，不阻塞前三个 tab。
- **历史研究产物处理规则**：`output/shortpick-market-factor-study*.json` 与 `output/shortpick-replay-factor-rank*.json` 是形成 live 策略的有效证据，保留为研究 artifact；结论压缩写入 `docs/archive/SHORTPICK_MARKET_FACTOR_CLOSEOUT_2026-05-09.md`。无效口径继续按既有结论处理：flash replay 不作为正式样本，LLM broad rejector/distillation 不升为默认策略，强制行业分散只作风险诊断。

## 2026-05-08

- **组合级与 regime 分解补齐后，强制行业分散不适合作为默认，regime gate 应进入下一阶段主线**：`shortpick-market-factor-study` 现在输出 `portfolio_summary` 和 `regime_summary`。组合级口径按每个 signal_day+horizon 的 Top6 等权组合计算，不再只看单票样本均值；同时记录 volatility、worst portfolio、additive drawdown、top symbol/industry concentration。新增 market-only 诊断策略 `ret10_turnover_cooldown_diversified`，在 cooldown 排序基础上默认单行业最多 2 只。2023-05-25..2026-04-30 等权基准结果显示：行业分散确实把 holdout top industry share 从 cooldown `70.1%` 降到 `32.9%`，但 alpha 基本被打掉，holdout 单票 trimmed `-0.083%`，组合 trimmed `+0.672%`，远弱于 cooldown 的单票 trimmed `+2.185%`、组合 trimmed `+2.752%`。因此强制行业分散只保留为风险诊断，不作为默认选股策略。regime 分桶更有用：按训练期 tertile 切分后，高 `universe_breadth10` 桶中 cooldown train portfolio trimmed `+0.23%`，holdout portfolio trimmed `+3.60%`；高 `pool_ret10_mean` 桶中 cooldown train trimmed `+0.11%`，holdout `+3.32%`；低/中宽度 regime 下仍显著更弱。已新增合成策略 `ret10_turnover_cooldown_regime_gate`：使用训练期 tertile 阈值，仅在 `universe_breadth10` 或 `pool_ret10_mean` 处于高桶时启用 cooldown。长样本训练期启用 `266/668` 个信号日，组合 mean `+0.493%`、10% trimmed `+0.028%`、positive `48.0%`；2026-03/04 holdout 启用 `16/43` 个信号日，组合 mean `+3.999%`、10% trimmed `+3.316%`、positive `75.0%`，优于 cooldown 全量组合 trimmed `+2.752%` / positive `69.1%`。当前主线：默认候选用 cooldown，仓位/启用用 regime_gate；ret10_turnover 保留为进攻型对照，行业分散保留为风险诊断。
- **cooldown reranker 验证：降低单日追高权重比历史先验更有效**：在 `ret10_turnover` 之后继续测试两类优化。第一类是 point-in-time 历史先验，只允许使用某只股票此前进入 Top40 且 forward window 已完成后的短投结果作为当日排序先验；代表参数均未改善长期稳健性，训练期 10% trimmed 仍约 `-0.94%~-1.07%`，且会吃掉 2026-03/04 holdout 收益，因此暂不固化。第二类是候选级交易约束，测试后新增 `ret10_turnover_cooldown` / `momentum_10d_turnover_cooldown_rank`：公式为 `rank_percentile(return_10d) + rank_percentile(turnover_rate) - 0.5*rank_percentile(return_1d)`，含义是保留 10 日持续性和换手确认，但降低信号日单日已经冲太猛的追高权重。扩到 2023-05-25..2026-04-30、等权 universe 基准后，训练期 base mean `-0.056%` / 10% trimmed `-0.890%`，ret10_turnover mean `+0.008%` / trimmed `-0.991%`，cooldown mean `+0.093%` / trimmed `-0.885%`；2026-03/04 holdout 中 cooldown mean `+3.755%` / trimmed `+2.185%` / positive `53.8%`，相对 base paired diff `+2.705%`、10% trimmed paired diff `+1.879%`、win `65.7%`。2024 起等权 study 结果一致：cooldown train trimmed `-0.914%`，holdout mean `+3.755%` / trimmed `+2.185%`；CSI300 2025 起敏感性为 train trimmed `+0.312%`，holdout mean `+4.043%` / trimmed `+2.432%` / positive `55.4%`，paired win `65.4%`。25 个 V4 Pro replay 日期里，纯 ret10_turnover 仍是最高均值：mean `+5.910%` / 10% trimmed `+3.972%`；cooldown 为 mean `+5.548%` / 10% trimmed `+3.781%`，但相对 base paired win 更高：`72.1%` vs ret10_turnover `70.1%`。当前解释：cooldown 是更稳健、更少追高的候选默认；如果只追求 2026 replay 绝对收益，纯 ret10_turnover 更强，但产品/研究默认应优先 cooldown，并继续标注 regime-sensitive。
- **scheduled-refresh 重试必须退避，避免夜间/研究时段每 5 分钟抢 SQLite 锁**：定位到 LaunchAgent 配置含 `StartInterval=300`，脚本即使在非盘中也会检查 postmarket/shortpick slot；只要 `.ok` 不存在，旧逻辑会每 5 分钟尝试补跑，若进程被外部 kill 还不一定来得及写 `.failed`，导致 `shortpick-lab-validate-recent` 或 daily refresh 在夜间反复占用 runtime SQLite。新增 `ASHARE_SLOT_RETRY_INTERVAL_SECONDS`，默认 `1800` 秒；`postmarket` 和 `shortpick_lab` slot 会先写 `.attempted`，最近尝试或失败都在退避窗口内直接跳过，避免未完成任务无节制重试。研究运行前仍应 bootout scheduled-refresh，结束后再 bootstrap；该改动是降低常态抢锁频率，不是替代研究期间的显式暂停。
- **ret10+turnover 复合排序已补入 replay 与扩样本研究，当前最强但仍是 regime-sensitive alpha**：在保留既有 LLM 直接选股、LLM 蒸馏、rejector、hard-veto、动量成交量和 factor-rank 数据的基础上，新增 `momentum_10d_turnover_rank` / `ret10_turnover`，核心公式为在 Top40 动量成交量池内按 `rank_percentile(return_10d) + rank_percentile(turnover_rate)` 选 Top6。同时把 market-factor study 的默认 benchmark 改成 `universe_equal_weight`，即对有完整 entry/exit 日线的 stock-like series 做等权 close-to-close 基准，另保留 `--benchmark-mode csi300` 做指数超额敏感性检查。25 个 DeepSeek V4 Pro replay 日期中，`10日动量换手复合排序` 当前最强：n=`518`，net mean `+5.910%`，10% trimmed net `+3.972%`，positive `60.2%`；相对原动量 Top6 的 paired diff `+2.905%`，10% trimmed paired diff `+1.994%`，paired win `70.1%`。扩样本验证中，2024-01 至 2026-04 等权基准 study 的 2026-03/04 holdout 也支持该方向：ret10_turnover mean `+3.640%`，10% trimmed `+2.050%`，positive `53.8%`，paired diff `+2.590%`，10% trimmed paired diff `+1.689%`，paired win `61.9%`；CSI300 2025-04 起 study 对应为 mean `+3.945%`，10% trimmed `+2.289%`，positive `54.8%`，paired diff `+2.562%`，10% trimmed paired diff `+1.665%`，win `61.2%`。但 2024-01 至 2026-02 train 在等权基准下所有策略 10% trimmed 仍偏弱，ret10_turnover train trimmed `-1.009%`，说明这不是无条件全 regime 稳定 alpha。额外测试简单市场涨跌阈值 gate（5/10/20/40/60 日 lookback，多阈值）未能修复 train 期弱稳健性，暂不采用。当前可靠结论：`ret10_turnover` 是下一阶段最值得保留的候选排序器，但应该按 regime-sensitive 候选处理；下一步重点是 regime 分类、更多时间/股票 universe 扩展、或严格证据层二次过滤，而不是先改前端包装。
- **benchmark 历史已补齐到可覆盖 2024，factor-rank 扩样本结论进一步校正**：运行前备份 runtime DB 到 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/data/ashare_dashboard.before-benchmark-backfill.20260508220922.db`，随后用 `sync_benchmark_index_bars(lookback_days=950)` 补齐沪深300/中证500/中证1000 指数日线，各 `625` 根，覆盖 `2023-10-09..2026-05-08`。新增可复用 CLI `sync-benchmark-index-bars --lookback-days`，避免后续再用一次性 Python one-liner。补齐后重跑 `shortpick-market-factor-study --start-date 2024-01-01 --end-date 2026-04-30 --cost-bps 20`：全段 `562` 个信号日、`16652+` 条 completed validation。更长 train 期显示因子优势显著收缩：base train net mean `+0.672%` / trim `-0.425%`；turnover `+0.752%` / `-0.583%`；ret10 `+0.700%` / `-0.491%`；combo `+0.709%` / `-0.505%`。但 2026-03/04 holdout 仍保持优势：base `+1.384%` / trim `+0.071%` / positive `45.2%`；turnover `+3.265%` / trim `+1.549%` / positive `51.0%`，paired diff `+1.888%`、win `63.4%`；ret10 `+2.741%` / trim `+1.342%` / positive `51.8%`，paired diff `+1.386%`、win `60.7%`；combo `+2.318%` / trim `+1.021%` / positive `49.8%`。原 25 日 replay window 依旧强：ret10 net mean `+4.570%` / trim `+2.841%`，paired diff `+2.264%`、win `69.9%`。敏感性检查 `--apply-limit-up-filter` 几乎不改变 holdout/replay 结论。当前更可靠的判断：2024-2026 全段不支持“无条件长期稳定强 alpha”，但 2026-03/04 regime 中 `turnover/ret10` 相对原动量确实更优；下一步应按市场 regime 或月度 walk-forward 选择，而不是把 25 日 replay 的高收益直接年化或当成常态。
- **短投 market-only 扩样本验证已落地，当前结论从“强收益”降级为“有优势但仍需继续扩样本”**：新增后端研究 CLI `shortpick-market-factor-study`，不写前端、不调用 LLM、不写候选表，只读取 runtime 日线，按官方短投口径近似验证：信号日后一个交易日收盘入场，`1/3/5/10/20` 交易日后收盘退出，默认扣 `20bps` roundtrip 成本；涨停不可成交过滤保留为可选敏感性检查。数据边界：runtime 沪深300 benchmark 从 `2025-03-31` 才有日线，因此正式超额收益扩样本使用 `2025-04-01..2026-04-30`，共 `263` 个信号日、`59` 条 stock-like series；`2025-04-01..2026-02-27` 为 train，`2026-03-01..2026-04-30` 为 holdout。扣成本后的 holdout 结果：base mean `+1.329%` / trim `+0.028%` / positive `45.0%`；turnover mean `+3.235%` / trim `+1.514%` / positive `50.9%`，paired diff `+1.909%`，paired win `64.1%`；ret10 mean `+2.709%` / trim `+1.315%` / positive `51.7%`，paired diff `+1.401%`，paired win `61.3%`；combo mean `+2.266%` / trim `+0.970%` / positive `49.7%`，paired diff `+0.946%`，paired win `59.7%`。在原 25 日 replay window 中，ret10 仍最强：net mean `+4.514%` / trim `+2.775%` / positive `54.8%`，paired diff `+2.314%`，paired win `71.4%`；turnover 与 combo 也明显强于 base。120 信号日 walk-forward 策略选择在 2026-03 选择 `ret10`、2026-04 选择 `turnover`，holdout net mean `+2.480%` / trim `+1.040%` / positive `50.6%`，paired diff `+1.147%`。敏感性检查打开 limit-up filter 后，holdout/replay 结论几乎不变。当前判断：`ret10` 和 `turnover` 可作为下一阶段主候选，25 日 replay 的高收益有行情放大成分，不能直接当稳定预期；更保守的可接受预期应看 holdout 扣成本后的 `+1.3%~+1.5% trimmed net excess`。
- **历史回放 factor-rank 组给出当前最强收益改进**：在不改前端、不调用 LLM 的前提下，先用 2024-01 至 2026-02 的可用日线做训练期筛查，再把 2026-03-26 至 2026-04-30 的 25 个 DeepSeek V4 Pro replay 日作为 holdout/runtime 写入期。结论是：原 `动量成交量Top6` 的主问题不是召回，而是 Top40 内排序太依赖单日涨幅；在扩大动量池内用高换手、10 日持续动量、短动量+5日持续+换手复合排序，均显著优于原动量 Top6。新增后端-only family：`momentum_turnover_rank`、`momentum_10d_rank`、`momentum_continuity_turnover_rank`，每组 25 run、150 candidates、144 official candidates。runtime official 读数：原动量 Top6 mean `+2.963%` / trim `+1.321%` / positive `50.6%`；换手优先 mean `+4.449%` / trim `+2.482%` / positive `53.0%`；10 日持续动量 mean `+4.973%` / trim `+3.186%` / positive `57.2%`；复合排序 mean `+4.704%` / trim `+3.049%` / positive `58.4%`。相对原动量 paired diff：换手 `+1.487%`、10日持续 `+2.056%`、复合 `+1.761%`；paired win rate 分别 `62.8% / 65.1% / 69.8%`。drop-best 稳健性：10日持续去掉最佳股票后仍 `+3.481%`，去掉最佳日期后仍 `+4.270%`；复合排序去掉最佳股票/日期后仍 `+4.490% / +4.514%`。当前解释：短投试验田更适合让 LLM 做封闭信息解释/硬否决/异常诊断，而把第一层 alpha 捕捉交给结构化市场特征；下一步若继续引入 LLM，应让模型在这些高质量 feature-ranked 候选上做少量、证据约束的二次判断，而不是从 raw 动量池直接主观筛掉股票。
- **历史回放 hard-veto 组已完成但不形成 alpha 增益**：在保留既有 `LLM原始选股 / 旧蒸馏 / 旧 rejector / 动量成交量` 数据的基础上，新增后端-only hard-veto 实验：`llm_hard_veto_then_momentum_rank`、`random_hard_veto_then_momentum_rank`、`llm_strict_veto_then_momentum_rank`、`random_strict_veto_then_momentum_rank`。第一版发现输入设计污染：动量候选没有个股专属来源时被塞入通用 official source，DeepSeek 会大量判成 `source_mismatch`，导致 hard-veto 变相惩罚“无来源动量股”；已修正为 hard-veto prompt 只暴露真实 linked source，并明确 `no_symbol_specific_source` 不能作为否决理由。修正版按 25 个 DeepSeek V4 Pro historical replay run、动量池 40、Top6、veto cap 10% 全量重跑完成，`historical_replay_momentum_pool_hard_veto` completed round 为 25。最终 LLM hard-veto 平均只否决 1.80 个/日，reason 分布为 `untradeable=41`、`liquidity_abnormal=3`、`negative_direct_conflict=1`，主要是指数符号 `000300.SH/000905.SH/000852.SH`；但这些否决没有改变 Top6 official 样本，`LLM硬否决后动量排序` 与 `动量成交量Top6` 完全同读数：mean `+2.963%`、trim `+1.321%`、positive `50.6%`、paired diff `0.000%`。结论：当前高精度硬否决对数据卫生有意义，但在现有 Top6 official 样本中没有贡献选股 alpha；旧 broad rejector 的负面结果主要来自过度主观过滤，而不是“LLM 否决机制必然有害”。
- **历史回放 rejector 新机制已在 runtime 重跑并发布验收**：按“扩大动量池召回、LLM 只剔除伪催化/弱来源、系统再按原动量成交量机械排序”的新设计新增三组：`llm_reject_only`、`llm_reject_then_momentum_rank`、`random_reject_then_momentum_rank`。运行态 25 个 DeepSeek V4 Pro replay run 全部完成 rejector round，扩大动量池从 500 条扩到 1000 条；最终候选覆盖为 `llm_reject_only=713`、`llm_reject_then_momentum_rank=125`、`random_reject_then_momentum_rank=125`，`historical_replay_momentum_pool_rejector` completed round 为 25。并发评估后采用 8 路并行：本机 8 核/16GB 下 LLM 调用期 CPU/内存占用很低，主要风险是 SQLite 写锁，因此不再上调。最终口径修正为 rejector 的 sources 只进入 `rejector_*` 元数据，不参与候选 official sample 审计；候选验证仍按动量池候选处理。最终读数：`LLM剔除后动量排序` robust mean `+1.155%`、trim `+0.919%`、positive `43.8%`，弱于 `随机剔除后动量排序` robust mean `+2.997%`、trim `+2.898%`、positive `48.1%`，也弱于原 `动量成交量` robust mean `+2.963%`、trim `+2.881%`、positive `50.6%`。这说明当前 rejector prompt 更像在过滤掉一部分真实动量强势样本，而不是稳定识别噪声。源提交 `c6b8509cebf8b94211399bfa3eed68c5d43f694e` 已发布到 runtime，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T124053Z-c6b8509cebf8/manifest.json`，deploy verifier `19 passed, 0 failed`。served Playwright 验收确认 `试验田 / 历史回放` 展示 `LLM只剔除保留池 / LLM剔除后动量排序 / 随机剔除后动量排序`，且请求 `/shortpick-lab/replay-feedback` 为 200、页面不再显示 `Not Found`，截图 `output/playwright/shortpick-replay-rejector-runtime.png`。前端聚合反馈请求改为 long-running timeout，否则 25 run 聚合计算会被 3 秒尝试超时 abort。
- **v4 pro 历史回放扩样本不能长时间锁住 runtime DB**：用户确认 DeepSeek token 够用后，先尝试用整段 `2026-04-07` 至 `2026-04-29` 串行补跑，但发现当前 CLI 在 LLM 调用前已写入 run 并持有 SQLite 写事务，导致 live DB 只读查询也可能 `database is locked`。本轮中止该长事务，仅保留已提交的 `2026-03-26`，随后修复 `shortpick_replay._run_one_replay_date()`：run shell 和 packet artifact 快速落库后立即 commit，v4 pro 等待期间不持有 DB 写锁，模型返回后再短事务写 round/candidates/validation 并 commit。提交 `33169b4436323da554f46922ed648b2d9ba60035` 已发布到 runtime，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T071510Z-33169b443632/manifest.json`，deploy verifier `19 passed, 0 failed`。
- **v4 pro 历史回放扩样本完成**：在锁库修复发布后，按 4 个分片并行补跑 `2026-04-07..04-10`、`2026-04-13..04-17`、`2026-04-20..04-24`、`2026-04-27..04-29`，加上此前已有的 `2026-03-26`、`2026-03-27..04-03`、`2026-04-30`，运行态现在共有 25 个 historical replay run，全部为 `deepseek-v4-pro[1m]`，flash replay round 为 0。当前 LLM 候选口径为 116 条候选记录、34 只去重股票、580 条 LLM horizon 验证行、451 条 completed LLM horizon 验证行；LLM+对照组总口径为 25 个历史日期、1,799 个 completed official 样本。`/shortpick-lab/replay-feedback` 仍按 `1/3/5/10/20` 返回 ready horizons。真实 served 页面 `http://127.0.0.1:5173/?verify=shortpick-v4pro-expanded-replay` 已验证显示 25 个历史日期、25 个回放批次、1,799 completed official 样本和 `deepseek:deepseek-v4-pro[1m]` 下钻行，截图 `output/playwright/shortpick-v4pro-expanded-replay-runtime.png`。
- **历史回放周期必须按研究口径排序，而不是字符串排序**：`validation_by_horizon`、`ready_horizons`、历史回放周期卡片和表格现在都固定按 `1/3/5/10/20` 展示。根因是后端把 horizon key 当字符串排序，导致 `1/10/20/3/5`；修复同时放在后端聚合和前端防御排序层，避免旧 payload 或未来接口乱序再次污染读数。
- **历史回放 DeepSeek 样本必须用 v4 pro，不接受 flash 作为正式能力验证样本**：确认运行态历史回放 7 个 real sealed-packet run 都是 `deepseek-v4-flash` 后，先备份 DB 到 `data/ashare_dashboard.before-v4pro-replay-rerun.20260508141936.db`，再删除 flash replay run/candidate/validation/consensus 与对应 artifact。随后发布提交 `3fe9385b274a1788589472041c891af3a1fe62ef`，将 `shortpick_historical_replay` 路由到 `deepseek-v4-pro[1m]`，并重新跑 2026-03-27、03-30、03-31、04-01、04-02、04-03、04-30 七个历史日。运行态 DB 现在只有 `deepseek-v4-pro[1m]` historical replay model rounds，共 7 run；`/shortpick-lab/replay-feedback` 返回 7 个历史日期、623 个 completed official 样本、ready horizons `[1,3,5,10,20]`，无 flash replay round。
- **v4 pro 历史回放发布验收记录**：release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T061821Z-3fe9385b274a/manifest.json`，deploy verifier `19 passed, 0 failed`。本地目标回归 `PYTHONPATH=src pytest -q tests/test_shortpick_replay.py tests/test_llm_service.py tests/test_frontend_shortpick_static.py` 为 `9 passed`，`ruff check` 通过，`npm run build` 通过，policy audit `status=pass`。真实 served 页面 `http://127.0.0.1:5173/?verify=shortpick-v4pro-horizon-order` 已用 Playwright+Node 24 验证：历史回放首屏显示 `1 / 3 / 5 / 10 / 20`，下钻表显示 `deepseek:deepseek-v4-pro[1m]`，截图为 `output/playwright/shortpick-v4pro-horizon-order-runtime.png`。Playwright CLI 用系统 Node 16 仍会触发 `GlobalRequest` 兼容错误，需临时把 bundled Node 放到 PATH 后再运行。

## 2026-05-07

- **参数与公式治理首轮要以股票看板为边界**：常量/公式治理已经按计划收口到数据质量、信号融合、Phase 5、短投验证边界和前端状态阈值，不做全工作区一次性大迁移。稳定 contract 留代码，公式进纯函数，可调权重/阈值/窗口进入版本化 `policy_config_versions`，每次运行 payload 记录 config key/version/checksum，后续新增参数必须经过 policy audit。
- **pytest 默认边界必须是快回归**：Phase 5 日刷、`refresh-runtime-data`、真实分析流水线、seeded workspace 和发布刷新级验收不再属于默认 `python3 -m pytest`。这类任务要拆成更小的单元/契约测试；确需跑完整链路时显式走 `runtime_integration`/专项验收。当前默认 pytest 已收窄到 `162 passed, 126 deselected in 17.37s`，避免普通开发收尾被日刷量级任务拖住。
- **参数与公式治理发布记录**：实现提交 `0cd28e04cff05adcbe50bde1f9ba44045c8f02fd` 已合入 `main` 并发布到 runtime，release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260507T145446Z-0cd28e04cff0/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态 API `/policy-governance/active` 与 `/policy-governance/audit` 均返回 `status=pass`；真实 served 页面 `http://127.0.0.1:5173/?verify=policy-governance-final` 已验证 `运营复盘 -> 治理与验收` 展示 `参数与公式治理`、5 个 code-default active 配置、0 个硬约束失败、公式/常量清单，截图为 `output/playwright/policy-governance-runtime-final.png`。
- **pre-push 没生效的根因和修复**：仓库 `core.hooksPath` 指向共享 `../../.githooks`，但该目录过去只有 `pre-commit`，没有 `pre-push`，所以 push 阶段完全没有门禁。现在新增版本化脚本 `scripts/hooks/pre-push-stock-dashboard.sh` 和安装器 `scripts/install-git-hooks.sh`，共享 `../../.githooks/pre-push` 会委托到项目脚本；门禁包括 clean worktree、推 main 时必须使用本地 main tip、默认 fast pytest、policy audit。
- **参数治理二次硬化**：active `policy_config_versions` 现在在 SQLAlchemy flush 前禁止原地修改 payload/schema/reason/checksum/审批字段，只能通过新增版本和 retire 旧版本演进；policy audit 同时拦截业务代码直接 `session.add/merge(PolicyConfigVersion)` 或原生 SQL delete/update/insert。operations overview 使用 compact policy governance projection，完整 payload/schema 通过治理 API 和 detail section 读取，避免治理数据继续推高首屏 payload。
- **参数治理二次硬化发布记录**：提交 `020b70094be84dc50f75dd37b92fb1f0da197883` 已 push 到 `origin/main`，真实 pre-push 已执行默认快回归 `164 passed, 126 deselected` 和 policy audit pass。runtime 发布 manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260507T160537Z-020b70094be8/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态 `/policy-governance/audit` 返回 `status=pass` 且硬约束失败为空；`/dashboard/operations` 的 `policy_governance.active_configs` 已不再携带 `payload` 大字段；真实页面 `http://127.0.0.1:5173/?verify=policy-governance-hardening` 已验证治理区域，截图为 `output/playwright/policy-governance-hardening-runtime.png`。
- **Claude 入口也必须覆盖同一套门禁**：`CLAUDE.md` 不能继续写旧的全量 `pytest tests/ -v` 口径，否则 Claude 会话会绕开快/慢测试分层和 policy governance closeout。现在 Claude 入口明确要求先安装共享 hooks、默认只跑 fast pytest、参数/公式变更必须跑 policy audit、runtime integration 用 `scripts/test-runtime-integration.sh` 专项触发、live-facing 改动必须 publish 并验证 served 页面。
- **Claude 门禁必须变成可失败检查**：仅把规则写进 `CLAUDE.md` 仍然可能被后续会话忽略或改坏。现在新增 `scripts/check-agent-constraints.sh` 与 `tests/test_agent_constraints.py`，并让 pre-push 与 GitHub Actions CI 都调用同一检查：Claude 入口必须包含 hook 安装、fast pytest、policy audit、runtime integration、publish/served 验证、origin/main containment 和 `policy_config_loader` 约束；旧的 `pytest tests/ -v` 口径会直接失败。
- **短投收益反馈要进入多 benchmark 计划**：试验田后验验证不能只展示沪深300超额；已单独立项 `SHORTPICK_LAB_MULTI_BENCHMARK_FEEDBACK_PLAN.md`，目标是在收益反馈表头切换 `沪深300 / 中证1000 / 同板块等权`。同板块维度必须真实构造同行等权基准；缺中证行情、缺板块映射或同行样本不足时显示待补原因，不能用绝对收益或沪深300静默替代。
- **短投多 benchmark 落地口径**：实现不改表结构，继续让兼容字段 `benchmark_return / excess_return` 表示默认沪深300；新增 `validation_payload.benchmark_dimensions` 保存三维结果。模型反馈聚合也从同一 map 计算 `benchmark_metrics`，前端用收益反馈表头控件切换展示，不能改变候选排序、验证状态或实验边界。
- **短投多 benchmark 已发布并真实验收**：实现提交已进入 `main` 并发布到 runtime；当前成功发布 manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260507T111724Z-211dc0291542/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态补跑 `shortpick-lab-validate-recent --days 30 --limit 20` 后，2026-05-05 批次已有 `10` 条 completed 验证快照，不再全部停在待窗口。API 验证确认 `/shortpick-lab/validation-queue` 的 `002384.SZ` 同时返回三维 benchmark：沪深300超额约 `+9.52%`、中证1000超额约 `+8.37%`、同板块 `C 制造业` 超额约 `+7.90%`；`/shortpick-lab/model-feedback` 也返回三维 `benchmark_metrics`。真实 served 页面 `http://127.0.0.1:5173/?verify=shortpick-multibenchmark` 已用 Playwright 验证 `试验田 -> 历史验证` 表头存在 `沪深300 / 中证1000 / 同板块` 切换，并且同一条 `2026-05-05 · 002384.SZ · 1日 · 已完成` 行会随切换显示不同 benchmark label 与超额收益。
- **短投收益反馈 UI、同板块同行样本和解析失败清理已发布验收**：修复提交 `77c6fab463b36fab89a90cdb2319ae3f626e62f1` 已合入 `main` 并发布到 runtime，manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260507T114943Z-77c6fab463b3/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。收益反馈表头切换改为 AntD Select 下拉框，历史验证 page-size 下拉层级高于下方卡片；同板块基准在现有同行日线不足时会按板块代表股票池补齐并固定拉取，当前已验证 completed sector rows 为 `available=10`，不再有 `pending_sector_peer_baseline` 或 `0/2` 样本状态。`validate_shortpick_run` 会清理已被成功结果取代的 `PARSE_FAILED` 候选，计划任务的短投 daily cycle 在生成 run 后自动执行 `shortpick-lab-retry-failed`。真实 served 页面 `http://127.0.0.1:5173/?verify=shortpick-ui-peer-parse` 已用 Playwright 验证：今日批次不再显示解析失败，历史验证只有 1 个 benchmark 下拉且没有 segmented 控件，page-size 菜单完整显示 `20 / 50 / 100 / page`，切到 `同板块` 后 `002384.SZ` 行展示 `C 制造业` 10 样本基准。
- **AntD Tabs 选中态不再做胶囊圆角**：全局 Tabs 样式过去给 `.ant-tabs-tab` 和 `.ant-tabs-ink-bar` 统一加 `999px` 圆角，导致 `试验田` 等切换式 tab 的选中态变成圆角块。现在全局改为 `border-radius: 0`，并补充 `ant-tabs-card` 覆盖，避免后续 card-style Tabs 重新带回圆角。本地任务预览 `http://127.0.0.1:5174/` 已用 Chrome headless 进入 `试验田` 验证，`今日批次 / 历史验证 / 模型反馈` 三个 tab 的计算圆角均为 `0px`，截图为 `output/playwright/tabs-no-radius-shortpick-local-final.png`。
- **停牌/缺行情短投候选不能进入正常研究池**：`600958.SH 东方证券` 出现在 2026-05-06 试验田批次，是因为模型抓到了“收购上海证券 + 5月7日复牌”的公开事件催化，原始实验记录可保留；但验证层已判定信号日后无可交易入场价、最新日线只到 2026-04-17，这类 `suspended_or_no_current_bar` 样本不能继续作为正常研究池候选展示或统计。
- **短投可交易性门禁必须写回候选分桶**：后验验证发现 `pending_market_data`、`pending_entry_bar`、`suspended_or_no_current_bar`、`entry_unfillable_limit_up`、`tradeability_uncertain` 时，将候选持久标记为 `tradeability_blocked` 并计算 `display_bucket=diagnostic`；前端研究池只展示 `display_bucket=normal`，异常候选进入失败/异常诊断区，保留来源与原始模型输出留痕。
- **发布脚本不能假设新 worktree 已有前端依赖**：`scripts/publish-local-runtime.sh` 在临时干净快照或新 worktree 中运行时，`frontend/node_modules` 通常不存在，直接 `npm run build` 会在 `tsc` 上失败。发布脚本现在在 build 前检查 `frontend/node_modules/.bin/tsc` 和 `vite`，缺失时先按锁文件执行 `npm ci`；runtime 的 `scripts/start-local-frontend.sh` 也补同样检查，避免 runtime node_modules 被清理或首次启动时失败。
- **发布脚本的 repo build 必须复用 frontend env**：runtime 前端 LaunchAgent 会 source `~/.config/codex/ashare-dashboard.frontend.env` 后执行 `npm run build`，其中 PATH 会切到 Node 22。发布脚本如果不用同一 env 先构建 repo，可能用另一套 Node 产出不同 JS hash，导致 served asset parity 误报 repo/runtime 不一致。发布脚本现在在命令检查和 build 前读取同一个 frontend env。

## 2026-05-06

- **短投试验田长期运行要有历史验证工作台**：每天 10 条推荐长期积累后，最新批次视图不足以回答“之前推荐验证在哪里看”。试验田页面必须拆为 `今日批次 / 历史验证 / 模型反馈`，历史验证走服务端分页，默认 50 条，按 candidate-horizon 展示 `1/3/5/10/20` 交易日状态和收益反馈。
- **短投失败不能混入正常研究池**：`PARSE_FAILED` 与 failed round 进入失败诊断区；DeepSeek/SearXNG 无搜索结果和 JSON 解析失败归为 retryable，模型配置缺失归为 configuration failure。重跑只处理失败轮次，旧失败 artifact id 与错误原因写入 retry history，不能删除失败痕迹。
- **短投历史验证工作台发布验收记录**：实现提交 `89e0de29a640724017020dc69665bda52c6c8636` 已合入 `main` 并通过临时干净快照 `/tmp/ashare-shortpick-history-publish.XbLpCS` 发布到 runtime；release manifest 为 `/private/tmp/ashare-shortpick-history-publish.XbLpCS/output/releases/20260506T135644Z-89e0de29a640/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态 API 已确认 `/shortpick-lab/runs` 返回 `2026-05-06` 批次 `operational_status=retryable_failures`、`retryable_failed_round_count=1`，`/shortpick-lab/validation-queue` 返回 95 条 candidate-horizon 验证行，`/shortpick-lab/model-feedback` 返回 GPT/DeepSeek 模型反馈。Chrome headless 桌面验收已验证 localhost `http://127.0.0.1:5173/` 与 canonical `https://hernando-zhao.cn/projects/ashare-dashboard/` 均可进入 `试验田`，并显示 `今日批次 / 历史验证 / 模型反馈 / 失败诊断 / 重跑失败轮次`；截图位于 `/tmp/ashare-shortpick-history-validation.png` 与 `/tmp/ashare-shortpick-history-canonical.png`。
- **短投试验田不能只存在手动入口**：盘后 daily 既要跑主分析，也要跑短投试验田的验证闭环。`run-scheduled-refresh.sh` 的 shortpick slot 默认启用，先执行 `shortpick-lab-validate-recent` 刷新近期旧批次的行情/基准复盘，再生成当天新 run；如确需停用，显式设置 `ASHARE_ENABLE_SHORTPICK_LAB=0`。
- **每日分析状态必须暴露子任务**：用户看到“每日分析已完成”时不能掩盖试验田未跑。`/dashboard/scheduled-refresh-status` 现在返回 `components`，前端展示 `主分析 / 试验田` 各自的成功、运行、失败或待补跑状态。

## 2026-05-05

- **短投试验田必须保持旁路隔离**：`shortpick_lab` 的 run/round/candidate/consensus/validation 只能进入独立表和独立 artifact，不能回写 `Recommendation`、`ModelResult`、watchlist、候选池或 simulation auto-model。后续任何把短投结果展示到单票/候选/模拟盘主链路的改动，都需要先补 `DECISIONS.md`。
- **原生联网实验不要被统一搜索器提前收敛**：本课题的主价值是观察弱引导、原生联网、全市场自由发现是否带来结构化系统之外的增量视角。系统可以记录 sources 和 raw output，但 v1 不替模型规定关键词或材料集合。
- **收敛不是 verified**：多模型/多轮一致只能提升研究池优先级。前端和 API 不得把 `high_convergence` 直接翻译成 `verified`、买入建议或主评分置信度；后验验证窗口完成前必须保持“待验证/研究优先级”语言。
- **短投试验田发布验收记录**：实现提交 `dc249b6b7bffa3f33ead2a80cd0c1afd3687426c` 已合入 `main` 并发布到 runtime，release manifest 为 `output/releases/20260504T200424Z-dc249b6b7bff/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。`GET /shortpick-lab/runs?limit=1` 返回空列表属预期，因为本轮未触发真实 GPT/DeepSeek 实验；`GET /dashboard/candidates` 仍返回主候选池原数据，未被短投结果污染。
- **短投试验田浏览器验收记录**：Safari 已验证 localhost `http://127.0.0.1:5173/` 与 canonical `https://hernando-zhao.cn/projects/ashare-dashboard/` 都显示一级入口 `试验田`，进入后可见 `短投推荐试验田`、独立研究课题/不作为交易建议/不进入主推荐评分边界文案、刷新/触发/补跑复盘控件和空批次状态。Playwright CLI 本轮因 daemon runtime 的 `GlobalRequest` 类型错误无法启动，改用 Safari 验收；结束后已运行 `scripts/cleanup-browser-automation.sh`。
- **发布脚本刷新分支仍有历史缺口**：首次发布期间 post-deploy refresh 打印 `scripts/publish-local-runtime.sh: line 204: PYTHON_BIN: unbound variable`，但构建同步、backend/frontend health、canonical/local parity 和 deploy verifier 均已通过。后续若只需要同步 docs/status 提交，可用 `ASHARE_PUBLISH_REFRESH_MODE=skip` 避开刷新分支；单独修发布脚本时应补 `PYTHON_BIN` 初始化。
- **短投实验不能长事务执行模型调用**：首次真实触发时，旧实现把 run/round 创建、GPT/DeepSeek 调用、聚合和验证放在同一个 SQLite 写事务里，导致 `/watchlist` 等普通读接口等待到前端超时，随后前端 fallback 到 Vite origin 并显示 HTML/404 类错误。修复提交 `dc27dfc871ce4bfea48c550049087b9ac7edd8ad` 改为 run 创建后立即 commit、每轮结束 commit，并让本地 preview 不再 fallback 到 `5173` origin 作为 API。发布 manifest 为 `output/releases/20260505T033059Z-dc27dfc871ce/manifest.json`，deploy verifier 为 `19 passed, 0 failed`；Safari 已确认 `http://127.0.0.1:5173/` 关注池和 `试验田` 页面恢复，`GET /watchlist` 为 200。
- **真实流程跑完才算新功能验收**：以后这类会产生真实 runtime 行为的新功能，不能只靠单测、构建、发布和页面可见判定完成；必须按约定流程至少真实执行一次，并看护到最终状态、关键副作用边界和前端展示都通过。短投试验田本次真实验收 run `shortpick:2026-05-05:20260505033458300263` 已完成：GPT/Codex 与 DeepSeek 各 5 轮共 `10/0` 成功/失败，`parse_failed=0`，生成 10 个研究候选、1 个 consensus、50 个待行情验证快照；Safari 显示 `试验田` 最近批次完成、综合优先级 `发散新颖`、候选均为 `待验证`；主 `recommendations`、`watchlist_follows`、`watchlist_entries` 对这些短投标的均未新增记录。
- **DeepSeek 官方 API 不能标记为原生联网**：真实探针已证明当前 `api.deepseek.com` 标准 API 即使带 `enable_search` 也不会提供 Web Search，旧 run `shortpick:2026-05-05:20260505033458300263` 中 DeepSeek 5 轮属于脏数据。短投默认 DeepSeek executor 改为 `deepseek_tool_search_lobechat_searxng_v1`：DeepSeek 先自主生成搜索查询，系统只用 LobeChat 的本机 SearXNG 执行查询，再把公开搜索结果喂回 DeepSeek 生成最终 JSON；若无可达来源或模型声称无法搜索，轮次必须失败并留 artifact，不能进入正常候选。

## 2026-05-04

- **已完成不能替代业务证据**：改进建议状态 `completed` 只能说明对应计划/任务已收口，不能直接让业务 gate 变为 `pass`。像“建议命中复盘覆盖”这类门禁必须重新读取真实 replay / benchmark / backtest 证据；若复盘记录、正式验证或组合补样本仍不足，即使计划任务完成也只能展示“治理计划已完成，但正式复盘口径仍待验证”的 `warn`。
- **看板不能展示过期中台任务快照**：股票看板读取改进建议时，需要用 `control_plane_task.id` 反查中台 `/api/tasks/:id` 的实时状态，并把 `publishVerified` / `workflowGates` 等状态投影到页面。控制面不可达时才保留 snapshot 状态并标记 `任务状态未实时同步`，避免本地建议文件里的旧 `blocked` 误导验收。
- **完成真值 gate 发布记录**：修复提交已合入 `main` 并发布为 `ba644dbc5f67d2606974ce7c8ed902080f427dfd`，release manifest 为 `output/releases/20260504T152200Z-ba644dbc5f67/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行时 API 显示“建议命中复盘覆盖”为 `warn`，文案为“治理计划已完成，但正式复盘口径仍待验证”；4 只股票数据质量仍为 `pass`，`financial_data_stale` / `profile_incomplete` 未再出现。
- **长耗时按钮必须有面板内持续状态**：`重新审计` 这类会触发双模型和后端生成的动作不能只等接口返回后弹 message；点击后必须立即在当前工作面板显示 `进行中` 状态，并让按钮进入 loading/disabled。成功或失败也要保留在面板内，避免用户无法判断任务是否已经启动。
- **重新审计不能复用短请求超时**：改进建议审计实际会调用 reviewer，前端请求策略必须使用通用长耗时 timeout，而不是运营面板详情加载的短 timeout；否则用户会看到“进行中”后很快失败，真实审计仍可能在后端继续跑。
- **AntD 弹框必须走主题上下文，不用静态 confirm 兜底**：`Modal.confirm(...)` 这类静态 API 会绕开当前 React 树里的 `ConfigProvider + darkAlgorithm`，在夜间模式下容易让弹框、Select dropdown、按钮和说明文字落到不一致的 token 环境。桌面入口应统一使用 AntD `App` provider，并通过 `App.useApp()` 下发 `message` / `modal`；新增确认弹框禁止再写裸静态 `Modal.confirm(...)`。
- **暗色主题优先收口到 AntD token 链路**：修组件可读性时不要用全局 `.ant-select-dropdown`、`.ant-modal-content`、`.ant-popover-inner` 这类选择器堆补丁替代主题系统。CSS 例外只保留业务布局、业务容器或非 AntD 原生结构；AntD 原生弹框、下拉和确认交互应由 `ConfigProvider` token 和上下文 modal 承担。
- **流程回归根因必须追到 mainline containment**：本次“进入计划池”夜间弹窗修复曾经存在于任务分支 `task/stock_dashboard/20260504-dark-modal-theme`，但没有进入 `main/origin/main`，后续主线发布覆盖 runtime 后导致用户刷新仍复现。此类回归不是单纯样式问题，而是 closeout 允许 branch-local fix 被当成完成；后续必须依赖 hook/backend gate 检查任务 commit 已被 main 与 upstream 包含。
- **暗色弹框回归最终收尾记录**：修复提交 `ec81aa819fe0200a41de2225ce5832b3a1932a73` 已进入 `main/origin/main`，发布 manifest 为 `output/releases/20260504T145606Z-ec81aa819fe0/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。Safari 已验证 localhost `http://127.0.0.1:5173/` 夜间模式下“进入计划池”模型选择弹框标题、说明、Select 当前值、任务说明和操作按钮可读；canonical `https://hernando-zhao.cn/projects/ashare-dashboard/` 已加载同一 runtime 页面与暗色主题。
- **验证冲突是解释优先级，不是普通指标**：RankIC 为负但正超额占比偏高时，用户首先需要知道“方向受益不等于排序能力成立”。这种冲突必须作为结构化字段从 API 保留下来，并在当前建议摘要、历史验证层、追问研究包、精简报告和移动端风险列表里排在普通风险或原始指标之前。
- **计划池提示词只能让用户审业务，不审代码**：改进建议进入中台 Plan 时，任务描述应把可审视内容限制在业务结果、风险边界、验收方式和真实外部决策。文件、字段、接口、测试命令、代码路径等实现细节由 AI 自行判断，只能作为内部参考，不能转成用户待回答问题。
- **600522 验证冲突收尾记录**：修复发布源提交 `54a0c2e18a43e54c054e5bac4bb072a08d8c370a`，release manifest 为 `output/releases/20260504T155249Z-54a0c2e18a43/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态 API 已确认 `historical_validation.validation_conflict`、`follow_up.research_packet.validation_conflict`、候选主风险和 copy prompt 均包含验证冲突；浏览器验收截图为 `output/playwright/600522-validation-conflict-local.png`。本轮还用中台 API 将原本模板化的计划反馈为业务可读计划，确认后续进入计划池的描述不应让用户审代码细节。

## 2026-05-03

- **数据质量 profile 完整性要复用全局板块规则**：`profile_incomplete` 不应因为历史 `profile_payload` 尚未落 `board` 字段而重复报警；只要 `board_rule()` 能根据 symbol/profile 给出已验证板块，就应作为质量检查的全局兜底。财报 freshness 仍必须来自真实财报快照或 feature snapshot，不能用调低阈值消除 `financial_data_stale`。
- **发布 parity 不应比较盘中同步计数文案**：`intraday_source_status.message` 会包含本地/canonical 各自触发同步后“新增或更新 N 根 K 线”的实时计数，属于运行时噪声。release verifier 应比较 provider/status/symbol/timeframe 等稳定结构，不应用该文案阻断无关代码发布。
- **收尾不能停在任务分支提交**：这轮漏项的根因不是发布或验收没做，而是 closeout 规范只盯住了“commit / publish / browser verify”，没有把“合回 `main` 并回到干净工作树”写成硬门禁。后续凡是用户语义接近“提交、收尾、完成”的场景，都要把最终 git 状态当成验收项：任务分支提交后，默认继续 `merge -> checkout main -> 确认 clean`；如果因 PR 流程或用户要求故意不合入，必须在回复中明确说“已提交但未合入”。
- **同源数据质量建议必须先聚合再审计**：`financial_data_stale`、`profile_incomplete` 这类公共数据链路缺口会同时影响多只股票，差异复盘不应把它们铺成一串逐股“数据质量为 warn”建议。后续建议收集应按 `degraded_sources` 签名聚合：先生成一条批量根因修复建议，修复后重新运行数据质量与改进建议审计，再把残留异常转为逐股处理。
- **旧审计快照也要读时聚合**：完整 `run_improvement_suggestion_review` 会触发双模型 reviewer，真实 runtime 上可能长时间阻塞。若只是为了消除旧 snapshot 中同源数据质量建议的重复展示，应在 `suggestion_details/summary` 读取快照时做投影聚合，而不是依赖一次新的完整审计先完成。

## 2026-05-01

- **计划池控制面边界必须先写死再开发**：这轮出现偏移的根因不是功能实现本身，而是“服务器入口”和“权威任务后端”在文档里没有被明确区分。当前约束固定为：股票看板创建 Plan 任务默认命中本机 control-plane `127.0.0.1:8787`；`/middle` 只可视为认证后的入口/观察面，不能被默认当成创建任务的 canonical backend。未来若要切远端权威后端，必须先改两边规范和运维流程，再改代码。
- **计划池动作必须创建可审视的中台 Plan 任务**：`accepted_for_plan` 不能停留在股票看板本地状态。按钮需要先让用户选择执行模型，然后通过中台 `/api/tasks` 创建 `planMode=true / approvalRequired=true` 的任务，并把页面中的建议、证据、双模型审计、最终判断和生成计划写入任务描述。中台返回的 task id 必须回写到 suggestion snapshot，方便股票页和中台互相追踪。
- **中台任务 schema 需要向前兼容新增字段**：股票看板创建任务时会传 `provider`，老的本地 SQLite `tasks` 表没有该列会导致控制面后端启动失败。`local-control-server` 的 schema 初始化必须为既有 DB 补 `tasks.provider TEXT NOT NULL DEFAULT ''`，不能只修改新建表结构。
- **公网中台验收受统一登录层约束**：无登录态 headless 浏览器访问 `https://hernando-zhao.cn/middle` 会被重定向到统一登录页，这是预期认证行为。此类场景需要至少保留本地中台 API 验收结果；若要声明公网页面可见，必须使用已登录浏览器会话或由用户侧确认。
- **deploy verifier 不能假设 shell 环境等于后端环境**：`ANTHROPIC_AUTH_TOKEN` 实际在 `~/.config/codex/ashare-dashboard.backend.env` 中，发布/验证脚本需要显式 source 后端 env 再执行 `llm_service.route_model(...)` 检查；文档和日志只记录“存在且可用”，不得记录 token 值。
- **SQLAlchemy 2.x verifier SQL 必须用 `text()`**：`Session.execute("SELECT 1")` 会在 verifier 中误报数据库不可用，正确检查是 `from sqlalchemy import text; s.execute(text("SELECT 1"))`。
- **前端 health check 不应 grep 过期品牌字符串**：构建后的 `index.html` 当前稳定信号是 `#root` 和 `assets/index-*`，不是旧的 `ashare-dashboard` 文案。verifier 应检查真实构建结构，而不是已经从 HTML 中移除的字符串。
- **持久化 recommendation payload 需要显示层向后兼容**：新闻因子生产逻辑已把 score 限制在 `[-0.98, 0.98]`，但 runtime DB 可能仍有旧 payload 的 `±1.0`。服务层和 factor card 层必须同时做用户可见钳制，避免专业用户看到“满分因子”误判为满置信。
- **发布刷新不能无限阻塞 verification**：`publish-local-runtime.sh` 现在支持 `ASHARE_PUBLISH_REFRESH_MODE=sync|async|skip`，同步刷新有 `ASHARE_PUBLISH_REFRESH_TIMEOUT_SECONDS` 外层超时；本轮最终发布使用 `skip`，因为 API 显示层修复已能直接验证当前已服务 payload。
- **最终收尾记录**：`PYTHONPATH=src python3 -m pytest -q` 为 `212 passed in 516.29s`；最终发布快照 commit 为 `a76683eb0d41ca6e6165abb429fbb4a6ceeec3f5`，runtime `latest-successful.json` 已更新；发布脚本内置 deploy verifier 为 `19 passed, 0 failed` 并输出 `VERIFICATION PASSED`；真实 Chrome served-page 验收通过，页面标题 `波段决策看板`，可见 `工作台 / 关注股票 / 复盘`，console error 为空，截图为 `output/playwright/runtime-verify.png`。

## 2026-05-02

- **运营复盘默认 tab 不能决定模拟盘是否拉取**：当前页面默认停在 `治理与验收`，而 `simulation_workspace` 过去只在切到 `模拟参数` tab 后才懒加载，导致首屏缺少模拟盘状态与双轨明细。复盘页首轮加载应保持 `summary-first`，但在 summary 成功后立即补拉 `simulation_workspace`；`portfolios`、`manual_queue` 等更重 section 仍按 tab 懒加载。
- **运营复盘模拟盘首屏拉取修复已发布并复验**：本轮通过临时快照仓 `/tmp/ashare-ops-prefetch.3XGKN1` 发布，release manifest 为 `/private/tmp/ashare-ops-prefetch.3XGKN1/output/releases/20260502T153722Z-40b27f0f1984/manifest.json`。发布脚本完成 runtime 同步、localhost 健康检查、parity verifier 与 deploy verification（`19 passed, 0 failed`）。浏览器侧复验分两路完成：1) Safari 真实登录态下访问 `https://hernando-zhao.cn/projects/ashare-dashboard/`，进入 `复盘` 后直接看到用户轨道/模型轨道与复盘卡片；2) Playwright 桌面视口访问 `http://127.0.0.1:5173/`，切到 `运营复盘` 时默认 tab 仍是 `治理与验收`，但页面已同步出现 `双轨同步模拟台`、`焦点 K 线`、`用户轨道`、`模型轨道`，无需再点击 `模拟参数` 才能看到模拟盘信息。
- **改进建议审计台的默认队列应排除已完成项**：`completed` 代表该建议已收口，不应继续占着“本周建议”默认队列造成“进入计划池”按钮灰掉但仍未出队的错觉。默认列表应只显示未完成项；若要追溯历史，需要单独提供 `已完成` 筛选，而不是把完成项继续混在待处理队列里。

## 2026-05-05

- **DeepSeek 官方 API 不能标记为原生联网短投 executor**：`/chat/completions` 路径没有 web-search 能力，`model_config.native_web_search=true` 不能只靠配置声明。短投实验的 DeepSeek 默认路径已改为 `deepseek_tool_search_lobechat_searxng_v1`：DeepSeek 先在隔离 prompt 中生成搜索计划，本机 LobeChat/SearXNG loopback 执行搜索，再把搜索证据喂回 DeepSeek 生成最终 JSON。旧的 OpenAI-compatible DeepSeek executor 在 shortpick 场景下直接 fail closed，避免无搜索能力时伪造来源。
- **脏 shortpick run 必须删除后重跑**：旧批次 `shortpick:2026-05-05:20260505033458300263` 依赖无联网 DeepSeek API，已从独立 `shortpick_lab` 表和 artifact 目录删除，不触碰 `Recommendation / ModelResult / Watchlist / PaperPortfolio` 主链路。新批次 `shortpick:2026-05-05:20260505064830869501` 已完成 10/0 轮：GPT 走 `isolated_codex_cli`，DeepSeek 走 `deepseek_tool_search_lobechat_searxng_v1`，DeepSeek 每轮都有搜索 query trace 与 SearXNG result count。
- **来源可信度当前只代表可达性与占位符拦截，不等于权威性**：新 run 的 sources integrity 结果为 `verified=41 / unreachable=4`，没有无来源 round；但是这还不是媒体/公告权威度评分。后续若提高研究质量，应在 URL 可达之后增加 domain/source authority 分层、公告/交易所/主流财经媒体优先级、以及低质量聚合站降权。
- **试验田验收必须跑完整 live flow**：本轮已发布、删除脏数据、真实触发新 run、检查 API schema、检查 DeepSeek 搜索 trace、检查 shortpick symbols 未进入 recommendations/watchlist，然后用 Safari 验证 `http://127.0.0.1:5173/` 和 `https://hernando-zhao.cn/projects/ashare-dashboard/` 均可进入 `试验田`，显示 2026-05-05 批次 10/0 完成，且页面保留“独立研究课题 / 不进入主推荐评分 / 不代表交易建议 / 未验证不得显示 verified”的边界文案。

## 2026-05-11

- **历史回放 DeepSeek v4 pro 严格来源补跑**：1 月至 3 月中旬的 sealed source packet 在 runtime DB 内 `official_source_count=0`，无法通过重跑变成严格来源样本；这些日期只能保留为可交易口径观察。真正可补的是 2026-03-27 至 2026-04-30 的有官方来源日期。本轮显式加载 `~/.config/codex/ashare-dashboard.backend.env` 并清代理后，用 `deepseek-v4-pro[1m]` 按单日限时方式补跑 `new_retail_cash_account` 口径，新增 run `96-120`，25 个交易日全部成功，`failed_llm_count=None`。批量并发曾因慢请求卡住，后续大模型历史补跑优先用单日/小批次可恢复执行。
- **默认历史统计必须避开失败补跑污染**：`/shortpick-lab/replay-feedback` 默认口径现在按日期只取“新开户账户口径 + 非诊断 + 有 LLM 候选 + LLM 轮次未失败”的最新 run。这样失败批次或空包补跑不会覆盖旧的有效 LLM 样本，也不会在前端重复计数。相关回归：`pytest tests/test_shortpick_replay.py -q` 为 `12 passed`。
- **策略组别补齐到同一有效日期集合**：在 54 个默认有效日期上补跑 `shortpick-replay-factor-rank`，把 `10日动量换手复合排序 / 10日动量换手降追高排序 / 10日持续动量排序 / 换手优先动量排序 / 持续动量换手复合排序` 等确定性策略组补回历史回放统计。served 页面 `http://127.0.0.1:5173/` 已用 Playwright CLI + Node v22 验证 `试验田 / 历史回放` 可加载，显示 `覆盖 54 个历史日期、54 个回放批次`、严格来源样本 `4426`、可交易验证样本 `11986`，当前领先组为 `10日动量换手复合排序`，LLM 原选仍单独显示为对照。
- **历史回放表格必须拆分候选验证与组合资金曲线**：`低换手上升趋势` 的长样本超额来自 5 万滚动组合回测，`10日动量换手复合排序` 的历史回放表现来自候选逐条后验验证；两者不能在同一列混排。前端 `策略收口和完整统计表` 已新增 `选股模式双口径对照`，每个选股模式按“候选逐条验证 / 组合回测”分列展示，并把完整明细拆成 `候选逐条验证` 与 `组合资金曲线` 两张表。对于只具备近似候选口径的组合变体，会显示 `近似参照`，避免误读为完全同口径证明。验证：`npm run build` 通过，`pytest tests/test_frontend_shortpick_static.py -q` 为 `2 passed`，served 页面已用 Playwright 展开 `试验田 / 历史回放 / 策略收口和完整统计表` 确认新表格可见。
- **双口径展示必须标清时间范围和统计对象**：`5日平均` 属于短窗口逐候选平均，回答“选股池平均质量”；`5万滚动资金曲线超额` 属于长样本账户路径回测，回答“按资金规则真实执行后的账户结果”。前端文案禁止出现“必须/不得/verified/builtin”等内部执行或开发指令式表达；用户可见层统一改为“短窗口候选统计 / 长样本账户路径回测 / 探索结果 / 默认模型”等可理解措辞。
- **短投研究原始产物不直接进入源码变更列表**：`output/shortpick-*.json/jsonl/db` 与 `output/external-reviews/` 是可复现研究运行产物，保留在本地 output 供审计，但默认不作为 git 待提交源码；长期结论写入 `PROCESS.md` / `DECISIONS.md` / `docs/archive/*`，必要的小型压缩归档单独跟踪。Playwright CLI 临时快照 `.playwright-cli/` 也进入 ignore，避免浏览器验收污染工作树。

## 2026-05-08

- **试验田前三个 tab 的 404 误报已修复并发布**：用户反馈 `今日批次 / 历史验证 / 模型反馈` 会一直显示 404。复现后确认 Short Pick Lab 接口本身正常，主要问题是外层 App 的后台单票/面板错误没有按视图隔离，进入 `试验田` 后仍把旧的 `面板加载失败 Not Found` 显示在试验田上方；另一个问题是 `历史验证`、`模型反馈` loader 失败后重试前没有清掉旧 tab error。修复为：导航切换时清空外层错误、后台 `loadDetailData` 在 `shortpick` 视图下不污染全局错误、`loadValidationQueue/loadFeedback` 开始加载时清空 Short Pick Lab 内部错误。源提交 `8faac6bb187a0e534c23e19e15a579dd12810de2` 已发布到 runtime，release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T111215Z-8faac6bb187a/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。served Safari 已验证 `今日批次`、`历史验证`、`模型反馈` 均可见数据且不再显示 404，截图 `output/playwright/shortpick-tabs-error-scope-runtime.png`。发布后自动触发的 `shortpick-lab-validate-recent` 一度占用 SQLite 导致短投接口阻塞，验收前已停止当次后台进程释放锁；后续仍建议批量短投验证和页面验收错开。
- **历史回放蒸馏扩展已发布并完成 served UI 验证**：源提交 `fb01f89162022b1aa776f256910137a643e4562b` 已发布到 runtime，release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T093603Z-fb01f8916202/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态 API `/shortpick-lab/replay-feedback` 已返回 `llm / llm_self_distilled / llm_momentum_distilled / momentum_volume_baseline / momentum_volume_expanded_pool / random_*` 全部 family；served Safari 页面 `http://127.0.0.1:5173/?verify=shortpick-replay-distillation` 的 `试验田 / 历史回放` 已验证可见 `LLM动量池蒸馏`、`LLM自选蒸馏`、`扩大动量池` 的汇总表与候选明细，截图 `output/playwright/shortpick-replay-distillation-runtime.png`。Playwright CLI 在本机 Node 兼容性上失败，Codex IAB backend 未发现，因此最终浏览器验收使用 Safari/Computer Use 完成。
- **历史回放扩展为三类研究对象**：在保留原有 25 个 DeepSeek V4 Pro sealed-packet replay run 的基础上，新增 `shortpick-replay-distill`，把历史回放扩成 `LLM原选 / LLM自选池蒸馏 / 动量成交量 / 扩大动量池 / LLM动量池蒸馏` 五类可比组。扩大动量池按每个回放日 20 只生成，运行态已有 `momentum_volume_expanded_pool=500` 条候选；V4 Pro 蒸馏在 token/env 修正后写入 `llm_self_distilled=31`、`llm_momentum_distilled=53` 条候选。纯本地 validation 已重刷 25 个 run 的 `1/3/5/10/20` horizon。当前读数：`LLM动量池蒸馏` 在 `1d=+1.183%`、`10d=+9.914%` 超额均值优于扩大动量池的 `+0.274% / +2.264%`，但 `5d=+2.049%` 仍低于原动量基准 `+2.435%`，`20d` 样本仅 10 个，不能下结论。补跑过程暴露两个流程问题：后端 env 必须显式 source `~/.config/codex/ashare-dashboard.backend.env` 才会用 `deepseek-v4-pro[1m]`，且本机 `phase5-daily-refresh` 定时进程会抢 SQLite 写锁；批量回放/蒸馏期间要先暂停或避开日刷。
- **历史回放不能只落 artifact，必须前端可感知**：本轮在隔离 worktree `20260508-historical-replay-workbench` 实现 `shortpick_historical_replay_v1` 首版闭环。新增 CLI `shortpick-replay`、只读 API `/shortpick-lab/replay-runs*`、sealed source packet、可交易 universe 快照、leakage audit 字段、三类 baseline、robustness metrics、IC gate 和新闻校准 readout；前端 `试验田` 新增 `历史回放` tab，显示 replay run、packet hash、official/diagnostic/rejected source、候选审计、source ids、LLM vs baseline、稳健性、IC 与新闻校准状态。
- **历史 replay 验证不得触发现时网络补数**：复用 `_upsert_validation_snapshot` 时发现同板块 benchmark 会扩展 sector peer universe 并触发 AKShare/资料查询，造成历史隔离测试卡住且有 current-data leakage 风险。修正为 replay 调用 `include_sector_benchmark=False`，sector 维度标记 `historical_replay_existing_only`；market data 与 benchmark sync 也保持 existing-only。
- **本地验证状态**：`python3 -m py_compile` 覆盖 changed backend modules；`pytest -q tests/test_shortpick_replay.py tests/test_factor_ic.py tests/test_frontend_shortpick_static.py` 为 `12 passed`；`pytest -q -m runtime_integration tests/test_shortpick_lab.py tests/test_shortpick_replay.py` 为 `28 passed, 2 deselected`；`npm run build` 通过。下一步仍必须发布到 runtime，并在真实 served 页面验证 `试验田 / 历史回放`，否则 live-facing 任务不能标记完成。
- **历史回放最终发布验收记录**：最终源提交 `ff8f06ad0e355d83ef4e3ba81c4e0a07b901e2ec` 已发布到 runtime，release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T021656Z-ff8f06ad0e35/manifest.json`，deploy verifier 为 `19 passed, 0 failed`。运行态生成 replay run `shortpick-replay:2026-04-30:20260508021056556401`：12 个候选、55 条 official source、4 条 rejected source、packet hash `91753f49ac80...`、4 类 LLM/baseline family、0 个 leakage failure；source excerpt 已截断到约 600 字，避免公告全文拖慢前端。localhost Playwright 验证 `试验田 / 历史回放` 显示 Replay 批次、Sealed Source Packet、LLM 与 Baseline 候选对照、泄漏审计、official/rejected 与 source packet，截图 `output/playwright/shortpick-replay-local-final.png`。真实 authenticated Safari canonical 路由 `https://hernando-zhao.cn/projects/ashare-dashboard/?verify=shortpick-replay-final` 已验证同一工作台可见，截图 `output/playwright/shortpick-replay-canonical-safari.png`。
- **没有 live shortpick run 时也必须显示历史回放**：canonical 验收暴露出 UI gating bug：`历史回放` tab 原本包在 `latestRun` 条件下，runtime 只有 replay run、没有 live run 时用户无法感知回放数据。最终修复为 `latestRun || replayRuns.length` 即展示 tabs，并在无 live run 时默认进入 `历史回放`。
- **历史回放严格审计后的补口径**：对 runtime run `5` 逐项审计 packet source 时间、候选 source id、evidence mapping、leakage audit、5 个 horizon validation、feedback family counts、主链路污染后，修正两类硬缺口：`build_shortpick_replay_feedback()` 的 family `official_sample_count` 从 validation-row 计数改为 candidate-level 计数；候选 payload 顶层补 `tradeability / market_cap_bucket / industry / limitations`，pending horizon validation 也补齐 `market_sync_status / benchmark_sync_status / benchmark_dimensions`。重跑严格审计已通过，仍保留一个数据质量警告：当前 runtime 这批 2026-04-30 样本的市值字段全为空，`random_same_market_cap_bucket` 只能生成 `unknown` 桶，不能被解读为有效市值分层对照；下一轮要补日级市值快照或把该 baseline 显式降级为 diagnostic。
- **历史回放 proxy 与市值桶修复**：`shortpick-replay` 的 LLM family 现在优先走真实 sealed-packet-only LLM executor，调用现有 LLM transport 时 `enable_search=False`，prompt 只包含 sealed source packet 与当日可交易 universe；若模型返回非 JSON，会用同一 sealed-packet 上下文做一次 JSON-only repair，仍失败才显式降级为 `historical_replay_diagnostic_proxy`，不再把 proxy 伪装成真实 LLM。市值桶从 `MarketBar.total_mv/circ_mv` 扩展到 raw/profile/seed fallback，并记录 `market_cap_source`；runtime run `7` 已验证为 `deepseek:deepseek-v4-flash / historical_replay_sealed_packet_llm`，同市值 baseline 三个候选分别来自 `large/mid/small`，不再全是 `unknown`。仍需后续补全全市场 point-in-time daily market-cap 数据，目前 runtime 只有 4 个 seed 市值来源，其余 universe 仍为 missing。
- **历史回放未来信息隔离审计加固**：LLM 候选引用 `rejected_sources` 或 packet 外 source id 时不再被规范化层静默替换为股票相关 official source，而是保留原始引用并触发 `source_after_cutoff / source_not_in_packet / unverified_source_time` 等 leakage audit failure，且这类候选不得进入 official sample。审计文本从 thesis 扩展到 catalysts/risks/invalidation。新增回归测试确认 sealed-packet prompt 不包含 rejected future source 的标题、URL 或 `rej-*` source id，并确认模型伪造 `rej-001/src-999` 会被隔离。对 runtime run `7` 的实际内容复审通过：official source after cutoff 0、prompt rejected-source mention 0、candidate packet-external source 0、official future-leakage violation 0。
- **历史回放首屏应是统计验收，不是单日细节**：用户指出 `历史回放` 只有 3 条 2026-04-30 run，没有解决“等待自然样本”的问题。补充全局 `/shortpick-lab/replay-feedback` 聚合接口，前端 `历史回放` 顶部改为跨 run 统计验收：历史日期数、completed official 样本数、1/3/5/10/20 horizon 完成样本、LLM/诊断代理/baseline 对照与基础统计门槛。单个 run、packet、source 明细降为下钻。运行态补跑 2026-03-27 至 2026-04-07 的历史 replay，并清理无 official 样本或早期验收残留 run `5/6/8/15`；当前聚合剩余 8 个 run，1日 94、3/5/10日各 84、20日 67 个 completed official 样本，统计门槛为 ready。注意：批量补样本使用 `historical_replay_diagnostic_proxy`，前端和聚合中单列为 `诊断代理`，不与真实 DeepSeek LLM run 混淆。
- **诊断代理不能作为正式历史回放验收样本**：诊断代理只保留为显式测试模式；真实模式 LLM 失败现在只记录 `historical_replay_sealed_packet_llm` failed round，不再自动生成 proxy 候选。全局 replay 列表与 `/shortpick-lab/replay-feedback` 排除 diagnostic proxy run。运行态已删除 proxy runs `9/10/11/12/13/14/16` 和无 official 样本的健康检查失败 run `17`，并删除对应 shortpick_lab artifact 文件；当前看板数据只剩真实 DeepSeek sealed-packet replay。
- **真实历史回放样本已补到可验收数量**：运行态新增真实 replay run `19-23`（2026-03-27 至 2026-04-02）和 run `18`（2026-04-03），均为 `deepseek:deepseek-v4-flash / historical_replay_sealed_packet_llm`，无 native web search，模型只读取 sealed packet。当前正式聚合共有 7 个真实 replay run，1/3/5/10/20 日均达到 ready：completed official 样本分别为 `134 / 124 / 124 / 124 / 100`，总 completed official validation 为 `606`，LLM family 有 `31` 个 official 候选，且无 `diagnostic_proxy_llm` family。
- **历史回放样本主体必须是 candidates，不是单个 primary_pick**：replay prompt 已改为要求 `candidates` 尽量填满候选上限且 `primary_pick == candidates[0]`，同时把 Anthropic-compatible 输出预算从固定 2048 提升到可配置默认 4096，避免为了补样本数导致 JSON 被截断后走失败路径。验证：`ruff check` 覆盖 changed backend/test files，`pytest -q` 为 `175 passed, 126 deselected`，`npm run build` 通过。
- **真实-only 历史回放发布验收**：提交 `34403dd19a96ff875b813064c63603d68bbef73a` 已 push 并发布到 runtime，release manifest 为 `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T040358Z-34403dd19a96/manifest.json`，deploy verifier `19 passed, 0 failed`。真实 served 页面 `http://127.0.0.1:5173/?verify=replay-real-only` 已用 Playwright 验证：页面无 `诊断代理`，显示 `deepseek:deepseek-v4-flash`，显示 `样本已达到基础统计观察门槛`，并显示 `覆盖 7 个历史日期、7 个 replay run；已完成 official 验证样本 606`。截图为 `output/playwright/shortpick-replay-real-only-runtime.png`。
- **试验田打开超时根因与修复**：根因不是后端查询慢，而是列表响应过重并且 live/replay run 混流。修复前 runtime `/shortpick-lab/runs?limit=20` 约 `3.9MB`，`/shortpick-lab/replay-runs?limit=20` 约 `2.6MB`，root 角色列表还带 raw/候选/source packet 明细，前端打开 `试验田` 时会同时拉取和解析多组大 JSON。现在 `/shortpick-lab/runs` 只返回 `native_web_open_discovery` 轻量 run 摘要，`/shortpick-lab/replay-runs` 只返回 historical replay 轻量 run 摘要，列表不再嵌入 candidates 和 full source packet；候选、source packet、raw answer 仍通过下钻接口取。发布提交 `240de6d784685912650d946888997fff6eb2cdf3`，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T050428Z-240de6d78468/manifest.json`，deploy verifier `19 passed, 0 failed`。发布后接口降为 `/shortpick-lab/runs?limit=20` 约 `161KB / 0.16s`、`/shortpick-lab/replay-runs?limit=20` 约 `74KB / 0.20s`；served 页面 `http://127.0.0.1:5173/?verify=shortpick-timeout-fix` 已验证打开 `试验田` 无 `请求超时`，截图 `output/playwright/shortpick-timeout-fix-runtime.png`。
- **历史回放页面必须先回答研究问题，再展示审计细节**：用户反馈“历史回放界面看不懂”后，本轮把 `历史回放` 首屏从 technical drilldown 改为能力验证读数：明确问题是“无上下文直接查询能否短投选股”，先展示当前统计门禁、历史日期/正式样本覆盖、`1/3/5/10/20` 日周期表现、LLM 与随机/同市值/动量对照组，再把回放批次、封闭数据包、当日股票池、泄漏审计和来源清单放到下钻区。提交 `5d086f25052c653f81a18ec007b2e4ed41270eb2` 已发布到 runtime，manifest `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard/output/releases/20260508T052042Z-5d086f25052c/manifest.json`，deploy verifier `19 passed, 0 failed`，post-deploy refresh 完成。Chrome 验证本机 `http://127.0.0.1:5173/?verify=shortpick-replay-clarity` 与标准入口 `https://hernando-zhao.cn/projects/ashare-dashboard/` 均显示 `历史回放核心读数 / 当前结论：可做初步统计比较 / 无上下文直接查询能否短投选股 / 1 / 3 / 5 / 10 / 20 日`，截图分别为 `output/playwright/shortpick-replay-clarity-runtime.png`、`output/playwright/shortpick-replay-clarity-canonical.png`。Playwright CLI wrapper 因本机 Node `v16.17.1` 与当前 npx 包不兼容失败，最终用 Chrome + Computer Use + `screencapture` 完成视觉验收，并已执行 `scripts/cleanup-browser-automation.sh`。

## 2026-04-30

- **专业化改造先落证据链，再落视觉专业感**：P-1/P0 已按“可审计、可解释、受控内测 beta”方向落到代码。新增 `data_quality_snapshot`、`factor_ic_study`、`weight_sweep_study`、CSI benchmark context 和 operations summary/details，前端只消费真实后端字段；P1/P2/P3 中需要样本积累或人工批准的 horizon/权重/毕业 gate 不做运行时动态切换，也不把当前样本不足的 study 包装成结论。
- **缺新闻不是硬风险，但仍是置信天花板**：数据质量层把无新闻覆盖记为 `data_coverage_gap:news`，评分保留在 warn 区间，不单独打 fail；producer 仍保持已批准的 positive signal watch ceiling，避免“缺事件证据但价格偏强”直接暴露为强方向建议。
- **operations 首屏预算需要接口拆分**：新增 `/dashboard/operations/summary` 和 `/dashboard/operations/details`，summary 会清空 portfolios/replay/simulation_workspace 等重字段并保留 today-at-a-glance/data-quality/factor summary。旧 `/dashboard/operations` 暂保留一个兼容期。前端首屏已经改成 summary-first；进入 `模拟参数` tab 时才懒加载 `simulation_workspace`/`portfolios` 明细。
- **P-1/P0 发布验收记录**：通过临时干净快照 `/tmp/ashare-professionalization-publish.757YdN` 发布到 runtime，manifest 为 `/private/tmp/ashare-professionalization-publish.757YdN/output/releases/20260430T163756Z-ddd8a9c561ce/manifest.json`。发布脚本的同步、backend health、frontend health 都通过，但 `verify-deploy.sh` 仍因 backend env 缺 `ANTHROPIC_AUTH_TOKEN` 在 LLM 模块检查处失败。实际 served-route browser 验证通过：operations 首屏使用 `/dashboard/operations/summary`、未提前调用 full `/dashboard/operations`，切到 `模拟参数` 后才调用 `/dashboard/operations/details?section=simulation_workspace`，单票证据页展示因子权重/贡献，console error 为空。
- **P-1/P0 最终收尾验证**：`2026-05-01T01:16:03+08:00` 重新跑 `npm run build` 通过，目标回归 `tests/test_professionalization_plan.py tests/test_dashboard_views.py tests/test_phase5_holding_policy_study.py tests/test_phase5_producer_contract_study.py tests/test_phase5_holding_policy_experiments.py tests/test_phase5_horizon_study.py tests/test_api_access.py -q` 为 `54 passed`；runtime smoke 显示 operations summary `20.8KB`、`portfolios=0`、`simulation_workspace=False`，detail endpoint 可取 `simulation_workspace=True`，单票页包含 `data_quality/factor_validation/benchmark_context`。
- **事件驱动分析的外部数据必须 fail-open**：AKShare `stock_individual_info_flow` 等外部接口可能在非交易时段返回空 DataFrame、超时或抛异常。`event_analyzer._fetch_external_data()` 的所有外部调用必须在 try/except 内，失败时返回空数据而非中断整个分析流程。prompt 中 `外部实时信息` 块显示"暂无外部实时信息流数据"比让整个 refresh pipeline 崩溃合理得多。
- **新模块集成到已有 pipeline 时要保护现有流程**：CLI `_refresh_runtime_data_output` 中的事件检测和分析调用包裹在 try/except 内，确保 trigger check 或 LLM 调用失败不会阻断后续的 intraday sync、simulation step 和 Phase 2 rebuild。
- **prompt 改动必须同步更新测试里的断言字符串**：`_follow_up_payload` 中 `目标 horizon` → `目标周期`、`系统当前结论` → `系统当前建议`、`验证样本量` → `回测样本量` 三处文案调整后，`test_dashboard_views.py` 的对应断言也会断裂。每次改 prompt 文案后跑一遍相关测试确认。
- **用 Python 脚本做文件替换时注意行结构**：直接 `result = lines[:411] + [new_body + "\n"] + lines[483:]` 会把整个函数体压成一行。正确做法是把 new_body 拆成 `list[str]`，每个元素是一行（含 `\n`），再用 extend/concatenate。

## 2026-04-29

- **多账号回归要先分清“真串号”和“展示误导”**：这次 canonical 用户反馈里，后端隔离其实已经成立，`member` 的 watchlist/simulation 都是空白；真正误导来自前端把“当前账号自选”和“全局候选池”继续混在一个工作台里展示。排查顺序应先用直连或 canonical API 验证账号态，再决定是修数据层还是修呈现层。
- **`act_as` 不应跨刷新保留**：root 代看别的账号空间时，如果把 `act_as` 持久化到 storage，用户稍后回到 root 页面会误以为“原持仓消失/复盘停止”。这类空间切换更适合单页内临时状态，刷新或重开标签后默认回到 actor 自己的空间。
- **canonical member 验证不一定非靠手点 dropdown**：当浏览器自动化无法稳定选中账号切换器时，可以用真实根域签名 session 直接请求 canonical `/projects/ashare-dashboard/api/*`，再带 `X-Ashare-Act-As-Login` 验证 edge 注入和成员空间返回值。对这轮问题，这条路径足以证明 `member-a/amoeba` 的真实 canonical watchlist 为空且 simulation 仍是 draft。

- **多账号隔离落地顺序**：先把身份上下文和数据归属改完，再改前端初始化。member 一旦还沿用旧的 `loadRuntimeSettings()` 启动顺序，就会在 `/settings/runtime` 上直接 403，造成整页白屏；正确顺序必须是 `/auth/context` → root 走 `/settings/runtime`，member 走 `/runtime/overview`。
- **关注池隔离不能等于分析断流**：自选列表要按账号隔离，但 symbol 级日更/分析资格必须按所有账号 active follows 的并集判断。移除某账号关注时只能删除该账号 follow；只有最后一个 follow 消失时，才允许全局 `watchlist_entries` 失去 active tracking。
- **模拟盘空白首登约束**：member 首次进入必须拿到独立 draft session，但 `can_start` 不能在空 `watch_symbols` 上误报 true。后端要同时在 workspace controls 和 `start_simulation_session()` 上都加护栏，避免 UI 隐藏了按钮但 API 仍可启动空 session。
- **局部回归要去掉重 fixture 依赖**：当前仓里已有一组与本次任务无关的 signal-engine fixture 回归（`build_signal_artifacts -> _fusion_state(...)` 参数漂移）。新增多账号测试如果继续复用 `seed_watchlist_fixture()`，会在进本次逻辑前先被旧回归拦住；这种情况下应改成最小自包含测试，只覆盖本轮 contract。
- **移动端设置 affordance**：只读状态行不能默认带右箭头；只有真实可操作且已接 handler/API 的项目才显示导航 affordance。二态偏好优先用 `Switch`，多选或模型选择才进入二级菜单。
- **移动端滑动操作视觉**：左滑删除这类 destructive action 的红色背景只能在展开状态出现；闭合态要避免卡片抗锯齿透出红色，展开态要取消卡片相邻侧圆角，避免出现双圆角边框。滑动释放后需吞掉下一次 click，防止误打开详情。
- **浏览器自动化恢复**：Playwright CLI 验收后必须运行 `scripts/cleanup-browser-automation.sh`。清理范围只包括 `playwright-core` daemon 和 `playwright_chromiumdev_profile-*` Chrome，不能 `pkill Chrome` 误伤用户普通浏览器。
- **移动端 app 化边界**：手机端不要把 PC 工作台继续压缩或在 `App.tsx` 堆 `isMobile` JSX。正确做法是 `App.tsx` 保持数据/handler 薄入口，移动端进独立 `components/mobile/*` shell；复盘页禁止复用 PC 宽表，必须用移动端卡片/列表表达同一数据。
- **Pydantic + future annotations 陷阱**：不要同时使用 `from __future__ import annotations` 和 `TYPE_CHECKING` 导入跨模块 Pydantic 类型。Pydantic v2 的 `model_rebuild()` 使用模块自身的 `sys.modules[module].__dict__` 解析前向引用，TYPE_CHECKING-only 类型不在该命名空间中。循环依赖时在 `__init__.py` 所有模块加载后注入类型并调用 `model_rebuild()`。
- **bash 脚本拼接陷阱**：不要用 `.join("; ")` 拼接数组生成 bash 命令，多余分号（如 `do;`、`then;`）会导致语法错误。改用字符串拼接，并对产物跑 `bash -n` 检查。
- **API 错误响应 key 约定**：JSON 错误响应用 `detail` 而非 `error`，前端 `core.ts` 只提取 `payload.detail` 展示给用户。key 用错会导致前端显示裸 HTTP 状态码而非中文提示。
- **LaunchAgent 配置规范**：定时任务用 `RunAtLoad` + `StartInterval`（或 `StartCalendarInterval`），服务进程用 `RunAtLoad` + `KeepAlive`。`StartInterval=300` 无法精确命中特定时钟分钟（如 08:10），需同时配置 `StartCalendarInterval` 数组。
- **SSH 隧道自愈**：隧道重连失败时先检查远端端口是否被旧 sshd 占坑；清理脚本失败时优先排查 bash 语法。隧道进程需自愈重连循环（指数退避），不能遇错即退。
- **策略冻结不等于跳过参数治理**：短投试验田里的“冻结策略”可以保持代码合同，但池子大小、TopK、阈值、惩罚系数、退出窗口和对照版本仍属于可调策略参数，必须先进入 `default_policy_configs.py` / `policy_config_versions` 可见配置，再由运行代码投影使用。policy audit 需要覆盖新策略域，否则模块常量会绕过原有门禁。

## 2026-04-28

- **live-facing 任务完成定义**：repo 改动 → 测试 → publish → runtime refresh → 真实浏览器验收。缺任一步都不能算完成。
- **浏览器验收优先用 Safari**。Chrome 出现旧缓存页/空白页/异常 profile，但 curl/health/assets 正常时，直接切 Safari 复核。
- **canonical "打不开" 三步排查**：(1) `localhost 5173/8000` 是否健康？(2) canonical 是否只是 302 回登录页？(3) `com.codex.project-tunnel.ashare-dashboard` 是否被远端旧端口占坑？
- **canonical stale 排查**：先查 tunnel / remote port ownership，不要先误判为 repo 或 runtime 代码失效。
- **DeepSeek timeout 排查**：先对照运行时 `urlopen(..., timeout=...)` 阈值，再判断 provider 故障。当前机器默认代理链路对 DeepSeek 比显式 no-proxy 更稳，不能想当然把"走代理"当根因。
- **simulation 刷新**：不允许只 restart session，必须 `restart → step → rebuild`。持续运行时用"交易时段后台 tick + 单次 anchored step"，不要复用研究刷新链路。
- **Phase 5 holding-policy artifact**：必须容忍 payload 里 legacy `backtest_artifact_id` 漂移，优先使用真实存在的 artifact。
- **historical validation**：不能复用 `as_of_data_time` 之后的 future exit bars，否则 horizon study 会被未来泄漏污染。
- **盘后 freshness 文案**：用户可见层用 `截至 MM/DD HH:MM` 快照表达，不显示原始秒数。
- **follow-up prompt**：不要把 recommendation 结论前置成"答案模板"，先给事实、验证状态和冲突要求，系统结论放最后。
- **运营复盘文案**：同一状态优先压成一条摘要，不在多个位置重复堆叠。
- **Safari 缓存**：Safari 可能保留旧标签页内存态，页面自相矛盾时先刷新再判断。
- **dirty worktree 发布**：应通过临时干净快照仓执行，manifest 路径写回 durable docs。

## 2026-04-27 之前

- **项目入口**：固定以 PROJECT_STATUS.json、DECISIONS.md 为主，不回到根目录散落 phase 文档模式。
- **"统一账号"三层边界**：谁发身份、谁消费身份、谁负责路由授权。任一层仍是单账号就不能描述为"已支持多账户"。
- **"系统能力"三层归属**：每项能力必须说明属于 Web 控制面、会话工具、还是本机 CLI/脚本，不能因中台有按钮就假设当前环境也有同样入口。
- **Phase 5 真值源**：以 runtime DB 为准，repo 本地库不再当作 live 评估的替代品。
