# Run 2026-06-13-W-004

## Run ID

2026-06-13-W-004

## Plan path

/Users/hernando_zhao/codex/worker-workspaces/stock_dashboard/20260613-fix-shortpick-10d-validation-refresh-d78369/plans/active/plan-20260613-shortpick-10d-validation-refresh.md

## Hop ID

W-004

## Work item ID

W-004

## Goal

Publish the verified source changes from this worktree to `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard` without running a refresh during publish.

## Non-goals

- Do not run validation refresh or runtime data mutation beyond the publish command in this hop.
- Do not verify 2026-05-26 10-day completion in this hop; that is W-005.
- Do not change source code in this hop unless publish exposes an in-scope packaging or verification issue.

## Plan evidence

- W-001, W-002, and W-003 are done.
- W-004 acceptance is `ASHARE_PUBLISH_REFRESH_MODE=skip bash scripts/publish-local-runtime.sh`.
- Live-facing project rules require publishing source changes to runtime before claiming completion.

## Files expected to change

- Runtime checkout under `/Users/hernando_zhao/codex/runtime/projects/ashare-dashboard`.
- `scripts/verify-shortpick-10d-runtime-refresh.sh` if needed to make W-005 acceptance executable before publish.
- Plan and run-record files under `plans/` and `runs/`.

## Implementation steps

1. Ask MiMo to confirm this publish run does not drift from the approved plan.
2. Ask Codex escalation reviewer for read-only review because this hop touches runtime/deployment behavior.
3. Add the W-005 runtime verification helper before publish so the runtime checkout receives it.
4. Commit the reviewed source, test, plan, run, and verification helper changes because `publish-local-runtime.sh` refuses dirty worktrees.
5. Run `ASHARE_PUBLISH_REFRESH_MODE=skip bash scripts/publish-local-runtime.sh`.
6. Record command exit evidence and continue to W-005 for runtime validation.

## Acceptance criteria

The publish command exits 0 and updates the local runtime source.

## Acceptance Type and Acceptance Spec

- Acceptance Type: `command_exit_0`
- Acceptance Spec: `cmd:ASHARE_PUBLISH_REFRESH_MODE=skip bash scripts/publish-local-runtime.sh`

## Planned Evidence

- MiMo run-plan review result.
- Codex escalation run-plan review result.
- Publish command exit 0.

## Actual Evidence

- `scripts/verify-shortpick-10d-runtime-refresh.sh` added and `bash -n scripts/verify-shortpick-10d-runtime-refresh.sh` exited 0.
- Codex escalation identified a publish-order blocker: `publish-local-runtime.sh` refuses dirty worktrees, so W-004 must commit before running the publish gate.
- MiMo verification-script review found no Blocking or Major issues.
- Initial publish attempts failed in deploy verification: first while waiting for `/health`, then while GET `/settings/runtime` timed out after 20s.
- Direct timing showed a full AKShare readiness import could take about 29.9s, exceeding the release verifier endpoint timeout.
- Added a settings-only bounded AKShare readiness probe (`timeout_seconds=2.0`) so `/settings/runtime` cannot block release verification on a cold AKShare import.
- Targeted/runtime settings gates passed: `tests/test_runtime_config.py` passed 8 tests; `akshare_runtime_ready(timeout_seconds=2.0)` returned in 0.796s; `python3 -m py_compile src/ashare_evidence/stock_master.py src/ashare_evidence/runtime_config.py` exited 0.
- Latest broad gates after the W-004 runtime settings fix passed: `python3 -m pytest -q` passed with 809 passed, 172 deselected, 6 subtests passed in 25.00s; policy audit exited 0 with status `pass`.

## Risk and rollback notes

Publishing writes to the local runtime project. The command uses `ASHARE_PUBLISH_REFRESH_MODE=skip` so it does not run refresh as part of publish. If publish fails, do not proceed to W-005 until the failure is understood.

## Gate plan

Run `ASHARE_PUBLISH_REFRESH_MODE=skip bash scripts/publish-local-runtime.sh`.

## MiMo plan-review result

MiMo run-plan review found no Blocking or Major issues for publishing with `ASHARE_PUBLISH_REFRESH_MODE=skip`.

## Codex escalation plan-review result

Codex escalation found two pre-publish blockers: the worktree was dirty and W-004 review state was still recorded as pending. Resolution: record the reviews, commit the source/run/plan changes, then run the publish gate from a clean worktree.

## Implementation summary

Added `scripts/verify-shortpick-10d-runtime-refresh.sh` so W-005 can be executed from the published runtime source. The script runs `shortpick-lab-validate-recent` with `--existing-market-data-only --horizon 10` and asserts 2026-05-26 completed 10-day snapshots for the four target paper-tracking roles.

Publish also exposed an existing runtime settings cold-path issue: `/settings/runtime` called `akshare_runtime_ready()` with an unbounded in-process AKShare import. Added an optional timeout-backed subprocess probe and changed only the settings display path to call `akshare_runtime_ready(timeout_seconds=2.0)`. Business paths that need real AKShare adapters still use the original in-process import behavior.

## MiMo code-review result

MiMo reviewed the verification helper and publish ordering. Result: Blocking = 0, Major = 0. It confirmed the helper does not fetch new market data and that commit-before-publish is required by the publish script dirty-worktree guard.

MiMo also reviewed the AKShare runtime settings timeout fix. Result: Blocking = 0, Major = 0, Minor only for the expected first settings-page cold-call latency capped at 2s. It confirmed core AKShare business paths are not routed through the subprocess probe.

## Codex escalation code-review result

Not required for business logic; Codex escalation run-plan review covered the publish-order blocker. The W-005 helper received MiMo focused review and `bash -n`.

## Gate results

- `bash -n scripts/verify-shortpick-10d-runtime-refresh.sh` exited 0.
- `python3 -m pytest -q tests/test_runtime_config.py` passed with 8 passed in 1.49s.
- `PYTHONPATH=src python3` probe showed `akshare_runtime_ready(timeout_seconds=2.0)` returned in 0.796s.
- `python3 -m pytest -q` passed with 809 passed, 172 deselected, 6 subtests passed in 25.00s.
- `PYTHONPATH=src python3 -m ashare_evidence.cli policy-audit --fail-on-new-unclassified --fail-on-direct-config-read --fail-on-formula-side-effects --fail-on-missing-config-lineage` exited 0 with status `pass`.
- Publish gate pending after committing the runtime settings timeout fix.

## Plan update summary

W-004 set to `in_progress` after W-003 completed.

## Plan archive result

Not applicable until all work items are complete.

## Archive and merge result

Pending.
