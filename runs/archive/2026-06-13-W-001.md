# Run 2026-06-13-W-001

## Run ID

2026-06-13-W-001

## Plan path

/Users/hernando_zhao/codex/worker-workspaces/stock_dashboard/20260613-fix-shortpick-10d-validation-refresh-d78369/plans/active/plan-20260613-shortpick-10d-validation-refresh.md

## Hop ID

W-001

## Work item ID

W-001

## Goal

Fix recent shortpick validation refresh selection and termination so mature pending 10-day paper-tracking snapshots are not stranded behind older no-progress pending rows, and prove the behavior with focused regression coverage.

## Non-goals

- Do not change frozen strategy parameters, roles, horizons, thresholds, or formula governance.
- Do not change frontend chart semantics or include pending rows as completed chart results.
- Do not publish to runtime in this hop; publishing is W-004.
- Do not run runtime validation refresh in this hop; runtime verification is W-005.

## Plan evidence

- Plan status changed to executing after explicit user approval.
- W-001 acceptance: `python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py`.
- Prior diagnosis: 2026-05-26 10-day runtime snapshots are `pending_forward_window` despite market bars being available beyond the required window.

## Files expected to change

- `src/ashare_evidence/shortpick_lab.py`
- `tests/test_shortpick_lab_validation.py`
- Plan and run-record files under `plans/` and `runs/`

## Implementation steps

1. Inspect `validate_recent_shortpick_runs` selection and loop termination around pending runs and latest runs.
2. Add a regression test where old pending rows make no progress but a later pending row can complete and must still be refreshed; confirm the new test fails against current code before applying the fix.
3. Adjust selection or loop termination so pending candidates within the lookback are not permanently skipped by an older no-progress slice.
4. Keep explicit bounds on selected runs and iterations to avoid unbounded daily refresh work.
5. Run the W-001 acceptance test.

## Acceptance criteria

The focused validation test file passes with its required runtime integration marker and includes coverage for old no-progress pending rows plus later matured pending rows.

## Acceptance Type and Acceptance Spec

- Acceptance Type: `test_pass`
- Acceptance Spec: `cmd:python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py`

## Planned Evidence

- MiMo run-plan review result.
- Code diff summary.
- MiMo code-review result.
- `python3 -m pytest -q tests/test_shortpick_lab_validation.py` exit 0.

## Actual Evidence

- Red phase confirmed: `python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py::ShortpickLabValidationTests::test_validate_recent_continues_past_old_no_progress_pending_slice` failed before the production fix because the mature pending run was not selected.
- Implementation changed `validate_recent_shortpick_runs` so pending/latest SQL queries exclude `processed_run_ids`, allowing later pending slices to be processed while preserving `run_limit` and `max_iter` bounds.
- MiMo code review reported no blocking or major issues and confirmed the fix solves the old-pending blocking mature-pending case while remaining bounded.
- Gate passed: `python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py` returned exit 0 with 17 passed in 1.87s.

## Risk and rollback notes

The main risk is widening daily validation refresh too far. The implementation must remain bounded by explicit limits and must avoid adding network fetch requirements to `--existing-market-data-only`.

Rollback is reverting changes to `src/ashare_evidence/shortpick_lab.py`, `tests/test_shortpick_lab_validation.py`, and this W-001 plan/run update.

## Gate plan

Run `python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py`.

## MiMo plan-review result

Passed. MiMo found no drift from the approved plan and no blocking or major risk. It suggested confirming the new regression fails before the fix; this run plan adopted that as part of implementation step 2. During execution, the acceptance command was corrected to include `-m runtime_integration` because the project pytest defaults deselect this file without the marker.

## Codex escalation plan-review result

Not required; this hop does not touch schema, credentials, security boundaries, runtime isolation, hooks, or irreversible governance state.

## Implementation summary

Implemented bounded catch-up for recent validation refresh. `_select_runs` now receives `processed_run_ids` and applies SQL-level exclusions to both latest and pending run queries. The previous "stop when no newly completed snapshots" guard was removed because it could halt after old no-progress pending rows before later mature pending rows were selected. The loop remains bounded by `max_iter` and by processing each run at most once.

## MiMo code-review result

Passed. MiMo found no blocking or major issues, confirmed the fix solves the diagnosed slice starvation, and noted only a non-blocking operational observation that each iteration can process up to `2 * limit` disjoint pending/latest runs.

## Codex escalation code-review result

Not required unless MiMo is inconclusive or finds material risk.

## Gate results

Passed: `python3 -m pytest -q -m runtime_integration tests/test_shortpick_lab_validation.py` exited 0; 17 passed in 1.87s.

## Plan update summary

W-001 set to `done` with evidence after implementation, MiMo code review, and focused test pass.

## Plan archive result

Not applicable until all work items are complete.

## Archive and merge result

Run document ready for archive; merge deferred until full plan completion.
