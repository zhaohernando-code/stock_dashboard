# stock_dashboard 当前生效决定

本文件只保留仍会影响产品、回测或下一轮研究的决定。被取代的逐轮过程不再复制到这里；需要追溯时查 Git 历史。失败研究的统一结论见 `docs/research/STRATEGY_RESEARCH_LEDGER.md`。

## 生产与纸面基线

1. V3 是永久核心基线，当前生产外部信息权重为 `lambda=0`。研究候选不得直接替换 V3。
2. 真实优化前沿为 Rank4-only；Rank5 不能生成买单，也不在纸面页面展示独立“影子观察”卡片。仅保留低成本前向观测数据，不能自动重开策略。
3. 纸面追踪展示三个 V3 角色和一个外部信息对照，四组统一从 2026-07-08、20 万元起算，并进入同一表格、曲线和交易明细。
4. 外部信息对照只能延长已有持仓退出，不能独立创建买入；历史回填、发布前重建段和真实前向必须保留不同证据标签。
5. 外部对照信号注册表 append-only：旧决策不能重写、删除或迟到回填；过期或无效时外部动作失败关闭，V3 不受影响。

## 个人可交易资格

1. 资格是任何打分和排序之前的硬过滤，不是分数惩罚。
2. 默认配置只允许普通 A 股主板；决策时点未复权可成交价格高于 200 元剔除；未开通的创业板、科创板、北交所等关闭。
3. “开户不足一年”不自动等于证券交易经验不足一年，因此权限必须是显式 profile，不把推测写死为监管事实。
4. 历史回测、纸面追踪和 live-facing 候选使用同一 eligibility snapshot，并记录每种剔除原因。
5. 禁止用未来价格或当前静态板块/ST 状态回填历史资格。

## 外部信息数据合同

1. 外部信息暂不进入 V3；先做渠道、数据固化、PIT 回放和独立验证。
2. 历史回测不得临时联网，硬条件是 `available_at <= decision_cutoff`。
3. Raw 原始响应不可变；Normalized 统一发布时间、首次发现、实际可用时间与修订；每个决策时点冻结 PIT snapshot。
4. 无法明确 `published_at`、`first_seen_at`、`available_at`、`revision_id` 或不可修订声明的渠道不得晋级。
5. 个人电脑只保存高相关摘要和元数据，不保存新闻正文；新闻研究存储上限为 2 GiB。
6. 官方事实、全球行情与专业新闻分层；公开源可以用于研究，但必须分别通过稳定性、相关性、时间戳、许可与修订链验收。

## 策略结构与权重

1. 股票固有数据保持主信号。外部信息只能作为有界残差或风险闸门，正面新闻不得独立触发买入。
2. 全球状态只控制总体风险敞口；板块状态只决定行业竞争与板块内排序；个股事件只提供个股增量。
3. 权重按 `0 -> 有界静态权重 -> 规则动态权重 -> 受约束学习` 逐级晋升；黑箱只可作为挑战者。
4. 最终选择“一标准误以内最小权重”的稳定平台，不选择收益最高的单点。
5. `lambda=0` 必须复现 NAV 与真实成交账本。现金释放产生的新买单、加仓挤掉后续买单、跨区间月份污染均不得计为 alpha。
6. 提前退出或延期持仓必须同时看共享现金账与冻结买入账；两本账不能同时通过时不得归因于外部信号。

## 2026-08-17 研究收口

1. 逐轮失败模型、脚本、测试、完整预测明细和重复预注册合同从主干移除；细节由 Git 历史保存。
2. 保留 V3、资格过滤、PIT 外部数据管线、纸面对照注册表、真实性门禁和一份 32,028 行全市场热点机会候选数据集。
3. 纯股票量价入口能覆盖生益科技和长电科技，但回归头与事件分类头都不能稳定排序；不再在当前复用窗口扫描股票阈值、目标或正则化。
4. 下一项允许的方向是先补齐 PIT 板块归属/状态、全球状态和高相关事件，再检验它们对已筛选股票路径的新增辨别力。
5. 在来源质量、PIT 覆盖或新独立样本不足时，继续积累数据和纸面证据，不创建新的策略实现。

## 2026-08-17 外部信息 PIT 准入边界

1. 全球/宏观紧凑快照和巨潮公告在 2023-07-13 至 2026-05-26 的共同窗口已达到“研究级 provisional”覆盖，可以启动股票路径、全球/宏观、官方事件、两者组合的四臂机会头机制筛选。
2. 这不是 V3 权重回测：保留候选没有完整 `core_score`，共同窗口内 `v3_soft_quality` 非零仅 36 条；任何结果只能说明外部信息是否改善热点机会头辨别。
3. 申万一级指数状态可以回放，但股票到行业只有当前静态标签，没有历史生效区间。禁止用 99.93% 的当前静态映射率回填历史；精确 V3 检验必须先补有效期化 PIT 板块归属和完整候选 `core_score`。
4. 专业新闻历史源仍为空；全球/宏观缺 revision/vintage，巨潮缺完整历史修订链。研究筛选即使为正也不得直接晋级生产，生产外部权重继续为 `lambda=0`。

## 2026-08-17 Tushare 结构化事件边界

1. Tushare `forecast_vip`、`express_vip`、`repurchase` 已通过账户权限和 HTTPS 实测；旧配置中的官方 `http://api.tushare.pro` 必须在发送令牌前自动升级为 HTTPS，令牌只允许发往官方 `api.tushare.pro` HTTPS 端点。
2. 2023-06-13 至 2026-08-14 已固化 71/71 批、31,197 条主板结构化事件，并通过 93,591 个文件的离线哈希回放；逻辑体积约 167 MiB、实际分配约 387 MiB，均低于 2 GiB。
3. Tushare 只有公告日期，没有供应商 revision ID 和日内发布时间；因此 `available_from` 保守设为公告日末，只能作为 provisional 研究源，不能成为生产级新闻修订链。
4. 预注册的业绩数值“水平残差”机会头筛选在校准段最佳为 15%，但 one-SE 仍选择 0%；不得加入 V3、纸面追踪或 live 候选。失败模型和测试不保留，采集、PIT 与离线回放基础设施保留。
5. 2026-05-27 至 2026-08-07 已被本轮作为独立结果打开；下一轮不能再把它称为 untouched。若研究“预告修订差/快报相对预告差”，必须等待新的未见前向结果或先补齐精确 V3 `core_score`，不得立即在同一窗口继续扫参数。

## 2026-08-17 精确 V3 核心与行业有效期边界

1. 活跃稳定盈利前沿的精确核心入口固定为 `negative_month_rank_weight_adjusted_capacity_cluster_v3_top3_20d_v1:trial-000`；`prefilter_score` 和 `v3_soft_quality` 不能再冒充 `core_score`。
2. 已为 V3 正式起点 2023-09-07 至 2026-08-14 物化 30,681/30,761 条候选精确核心分数（99.74%）。起点前 1,267 条不是 V3 历史；冻结矩阵未包含的 80 条不得事后用当前元数据补算。
3. 重叠 PIT 矩阵按最早冻结版本优先，后生成矩阵只做差异审计。本次 1,575 条重叠记录的特征谱系不同，但精确核心分数差异为 0。
4. Tushare 申万 2021 成分已固化 62 个无令牌 Raw 响应和 7,899 条有效期记录，覆盖 5,895 个证券、31 个一级行业、无区间重叠；这解决“当前静态行业回填”的 provisional 研究缺口。
5. `in_date/out_date` 只证明行业有效期，不证明历史 `published_at/first_seen_at/revision`。因此有效期模式可做机制研究，严格 `available_at <= decision_cutoff` 的历史 PIT 仍失败关闭；不得据此启动 V3 权重回测或生产权重。

## 2026-08-18 受限静态权重结论

1. 已按预注册合同完成精确核心受限机制诊断；它使用 provisional 有效期行业归属和同窗复用结果，结论上限不是严格生产组合回测。
2. 事件、板块、事件+板块的 one-SE 冻结权重在校准期为正，但验证期和复用评估期均转负；全球与三通道组合均选择 `lambda=0`，5 个外部臂全部未通过方向门禁。
3. 当前静态外部权重停止实现，不进入 V3、纸面追踪或 live 候选。临时运行模块与专项测试不保留，预注册合同和审计报告保留。
4. 不得在已打开的相同窗口继续扫描权重、窗口或模型形式。若重启，只能预注册状态依赖的前向风险开关，并同时保留 `lambda=0` 对照；严格历史行业可用时间与供应商修订链仍是生产前硬缺口。

## 权威来源

- 当前状态：`PROJECT_STATUS.json`
- 失败方向、关键指标与重新开启门槛：`docs/research/STRATEGY_RESEARCH_LEDGER.md`
- 保留数据及哈希：`docs/contracts/STRATEGY_RESEARCH_RETENTION_CONTRACT_2026-08-17.json`
- 工程与回测防回归原则：`PROCESS.md`
- 详细历史：Git commits；不再回填到本文件。
